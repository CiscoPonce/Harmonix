# Browser → API base (include /api). Use relative /api when Next proxies to the backend.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Capacitor / production builds often point at the public ngrok or domain:
# NEXT_PUBLIC_API_URL=https://moral-sparrow-nationally.ngrok-free.app/api
