# Remote URL loaded by the Capacitor WebView (production domain).
# Override for local/dev testing if needed.
CAPACITOR_SERVER_URL=https://harmonix.peeporunclub.co.uk

# Next.js API base — leave unset in Capacitor mode (same-origin /api).
