# Remote URL loaded by the Capacitor WebView (Harmonix VPS via ngrok).
# Override when ngrok URL changes or for local dev testing.
CAPACITOR_SERVER_URL=https://moral-sparrow-nationally.ngrok-free.app

# Next.js API base — leave unset in Capacitor mode (same-origin /api via ngrok).
# NEXT_PUBLIC_API_URL=/api
