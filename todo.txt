# Harmonix ops notes (VPS)

## Production
Public: https://harmonix.peeporunclub.co.uk
Checkout: /home/ubuntu/lyric
Deploy: git push origin main → GitHub Actions → scripts/coolify-redeploy.sh
Manual: cd /home/ubuntu/lyric && git pull origin main && docker compose build && docker compose up -d

Pocket-TTS: systemd unit harmonix-tts (:3002). Do not use run_env.sh except as rollback.

Grant admin: `UPDATE users SET is_admin = 1 WHERE email = 'you@example.com';` (no auto-promote from email text).

## Play Store
Flutter only (`mobile/`). Privacy policy: https://harmonix.peeporunclub.co.uk/privacy
