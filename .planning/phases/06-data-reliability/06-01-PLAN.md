---
phase: 06-data-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/db.js, server/routes/validation.js, server/services/validationService.js, server/services/validationService.test.js, server/routes/validation.test.js]
autonomous: true
requirements: [PLAYER-03]
user_setup:
 - service: None
env_vars: []
must_haves:
 truths:
  - "System validates lyric timestamps against song duration before serving"
  - "Bad songs are flagged in database and excluded from future recommendations"
  - "Cache hit ratio improves by storing validated song metadata"
 artifacts:
  - path: "server/services/validationService.js"
   provides: "Validation logic for lyric/audio sync"
  - path: "server/routes/validation.js"
   provides: "Validation API endpoints"
 key_links:
  - from: "server/services/validationService.js"
   to: "server/routes/vocab.js"
   via: "validation check before extraction"
---

<objective>
Implement the validation loop that ensures content integrity by screening out songs with mismatched lyrics and audio timing, and by optimizing caching.

Purpose: To prevent users from experiencing poor-quality content and to improve system reliability.
Output: Validation service, song quality cache, and background validation hooks.
</objective>

<context>
@.planning/phases/05-retention-gamification/05-01-SUMMARY.md
@server/db.js
@server/routes/vocab.js
</context>

<tasks>

<task type="auto">
 <name>Task 0: Create Test Scaffolds</name>
 <files>server/services/validationService.test.js, server/routes/validation.test.js</files>
 <action>
 Create empty test files with basic Mocha/Chai imports.
 </action>
 <verify>
 <automated>ls server/services/validationService.test.js server/routes/validation.test.js</automated>
 </verify>
 <done>Test scaffolds created.</done>
</task>

<task type="auto">
 <name>Task 1: Validation Schema & Cache Tables</name>
 <files>server/db.js</files>
 <action>
 Add tables:
 1. `validated_songs` (song_id PK, artist, title, duration, lrc_valid, lrc_checked_at, score)
 2. `song_cache` (song_id PK, lyrics_json, track_json, cached_at, expires_at)
 Optimize for read-heavy access patterns.
 </action>
 <verify>
 <automated>sqlite3 server/lyricword.db ".schema validated_songs" | grep "lrc_valid" && sqlite3 server/lyricword.db ".schema song_cache" | grep "expires_at"</automated>
 </verify>
 <done>Validation and cache tables created.</done>
</task>

<task type="auto">
 <name>Task 2: Validation Service</name>
 <files>server/services/validationService.js, server/services/validationService.test.js</files>
 <action>
 Implement `server/services/validationService.js`:
 1. `validateSongSync(trackData, lrcData)` → returns { valid, issues: [], score }
 2. Checks: LRC exists, timestamps are within song duration, no negative timestamps, reasonable line count.
 3. `cacheSongData(songId, lyrics, track)` → stores in song_cache with TTL.
 4. `getCachedSong(songId)` → returns cached or null.
 5. Write tests for validation logic and caching.
 </action>
 <verify>
 <automated>npm --prefix server test server/services/validationService.test.js</automated>
 </verify>
 <done>Validation logic correctly identifies bad sync data.</done>
</task>

<task type="auto">
 <name>Task 3: Validation API & Pre-flight Hook</name>
 <files>server/routes/validation.js, server/routes/validation.test.js, server/routes/vocab.js</files>
 <action>
 1. Create `server/routes/validation.js`:
    - GET /api/validation/status/:songId → returns cached validation status
    - POST /api/validation/check/:songId → triggers synchronous validation
 2. Integrate pre-validation in `server/routes/vocab.js`:
    - Before AI extraction, check `validated_songs` table.
    - If `lrc_valid = false`, return 409 with "Song quality too low for learning".
    - If not validated, run validation, cache result, then proceed.
 3. Write tests for the validation flow.
 </action>
 <verify>
 <automated>npm --prefix server test server/routes/validation.test.js</automated>
 </verify>
 <done>Validation hooks prevent bad songs from entering the learning flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend -> External APIs | LRCLib and Deezer requests |
| Backend -> Cache | Song metadata storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | /api/validation/check | mitigate | Cache results, rate limit AI extraction |
| T-06-02 | Tampering | song_cache | mitigate | Server-side only, no client writes |
</threat_model>

<verification>
1. Run unit tests.
2. Test flow: request vocab for a song with bad LRC → should return 409 or validation warning.
3. Verify `validated_songs` table is populated.
</verification>

<success_criteria>
System actively screens out poorly synced or incomplete lyric data before serving to users.
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-reliability/06-01-SUMMARY.md`
</output>
