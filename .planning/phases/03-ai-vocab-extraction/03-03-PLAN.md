---
phase: 03-ai-vocab-extraction
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [client/src/components/LyricList.tsx, client/src/components/VocabPopover.tsx, client/src/components/CefrSelector.tsx, client/src/app/player/[id]/page.tsx, client/package.json]
autonomous: false
requirements: [AI-01]

must_haves:
  truths:
    - "Target vocabulary words are visually highlighted in the lyrics"
    - "Clicking a highlighted word opens a definition popover with a 'Report Error' button"
    - "Unmapped vocabulary words are displayed in a 'Words in this song' sidebar"
    - "User can change their proficiency level via a selector UI"
  artifacts:
    - path: "client/src/components/VocabPopover.tsx"
      provides: "Definition UI with report button"
    - path: "client/src/components/CefrSelector.tsx"
      provides: "Proficiency selection"
  key_links:
    - from: "client/src/components/LyricList.tsx"
      to: "client/src/components/VocabPopover.tsx"
      via: "React Component"
---

<objective>
Implement the interactive frontend elements for vocabulary learning, including word highlighting, context-aware definition popovers with error reporting, proficiency selection, and a fallback sidebar for unmapped words.

Purpose: To provide users with a seamless and interactive way to learn new words while listening to music, tailored to their level.
Output: Enhanced LyricList, new VocabPopover, CefrSelector, and updated Player page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-vocab-extraction/03-02-SUMMARY.md
@client/src/components/LyricList.tsx
@client/src/app/player/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Radix Popover, CefrSelector & Highlighting</name>
  <files>client/package.json, client/src/components/LyricList.tsx, client/src/components/VocabPopover.tsx, client/src/components/CefrSelector.tsx</files>
  <action>
    1. Install `@radix-ui/react-popover` and `@radix-ui/react-select` in `client`.
    2. Create `client/src/components/VocabPopover.tsx`:
       - High-contrast design (black bg, white border).
       - Include word, lemma, definition, and CEFR level.
       - Add a "Report Error" button (per checker warning).
    3. Create `client/src/components/CefrSelector.tsx`:
       - A dropdown to select CEFR level (A1-C2).
       - Should update the user's `cefr_level` in the database (create a simple `PUT /api/user/profile` endpoint if needed, or handle in a future task). For now, focus on UI and state.
    4. Update `LyricList.tsx` to:
       - Accept `mappedVocab` prop.
       - Render segments, wrapping vocabulary words in a styled span that triggers `VocabPopover`.
  </action>
  <verify>
    <automated>npm --prefix client list @radix-ui/react-popover && grep -q "Report Error" client/src/components/VocabPopover.tsx</automated>
  </verify>
  <done>UI components for vocab interaction and proficiency selection are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Player Page Integration & Sidebar</name>
  <files>client/src/app/player/[id]/page.tsx</files>
  <action>
    1. In the player page, fetch vocabulary from `/api/vocab/:id` on mount.
    2. Pass the `mapped` vocabulary to `LyricList`.
    3. Implement a "Words in this song" sidebar or section to display the `unmapped` vocabulary items (per checker warning).
    4. Integrate the `CefrSelector` into the player page or a settings modal.
  </action>
  <verify>
    <automated>grep -q "unmapped" client/src/app/player/\[id\]/page.tsx</automated>
  </verify>
  <done>Player page displays both highlighted lyrics and a fallback sidebar for unmapped words.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive vocabulary highlighting, definition popovers, CEFR selector, and unmapped words sidebar.</what-built>
  <how-to-verify>
    1. Navigate to a song player page.
    2. Verify that target words are highlighted in lyrics.
    3. Click a highlight; verify the popover shows the definition and the "Report Error" button.
    4. Verify the "Words in this song" sidebar exists and lists words that aren't highlighted in the lyrics.
    5. Change the CEFR level using the selector and ensure the UI reflects the change.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> User | Rendering definitions and lyrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Cross-Site Scripting (XSS) | LyricList.tsx | mitigate | Use standard React string rendering |
| T-03-07 | Information Disclosure | CefrSelector.tsx | mitigate | Ensure user profile updates are authorized |
</threat_model>

<verification>
1. Verify Radix UI installation.
2. Manually verify the popover interaction, "Report Error" button, and sidebar.
</verification>

<success_criteria>
User can interact with all vocabulary (mapped and unmapped) and manage their proficiency level.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-vocab-extraction/03-03-SUMMARY.md`
</output>
