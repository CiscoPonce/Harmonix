---
phase: 03-ai-vocab-extraction
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [client/src/components/LyricList.tsx, client/src/components/VocabPopover.tsx, client/src/app/player/[id]/page.tsx, client/package.json]
autonomous: false
requirements: [AI-01]

must_haves:
  truths:
    - "Target vocabulary words are visually highlighted in the lyrics"
    - "Clicking a highlighted word opens a definition popover"
    - "Definitions are contextually relevant to the song"
  artifacts:
    - path: "client/src/components/VocabPopover.tsx"
      provides: "Definition UI"
    - path: "client/src/components/LyricList.tsx"
      provides: "Highlighting logic"
  key_links:
    - from: "client/src/components/LyricList.tsx"
      to: "client/src/components/VocabPopover.tsx"
      via: "React Component"
---

<objective>
Implement the interactive frontend elements for vocabulary learning, including word highlighting and context-aware definition popovers.

Purpose: To provide users with a seamless way to learn new words while listening to music.
Output: Enhanced LyricList component and new VocabPopover component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-vocab-extraction/03-02-SUMMARY.md
@client/src/components/LyricList.tsx
@client/src/app/player/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Radix Popover & Highlighting Logic</name>
  <files>client/package.json, client/src/components/LyricList.tsx, client/src/components/VocabPopover.tsx</files>
  <action>
    1. Install `@radix-ui/react-popover` in `client`.
    2. Create `client/src/components/VocabPopover.tsx` using the high-contrast design from RESEARCH.md (black bg, white border).
    3. Update `LyricList.tsx` to:
       - Accept a `vocab` prop (array of mapped words).
       - Implement a helper to split line text into segments based on vocab matches.
       - Render segments, wrapping vocabulary words in a styled span (e.g., dotted underline) that triggers the `VocabPopover`.
  </action>
  <verify>
    <automated>npm --prefix client list @radix-ui/react-popover && grep -q "VocabPopover" client/src/components/LyricList.tsx</automated>
  </verify>
  <done>Lyrics render with highlighted vocabulary words and Radix popovers are ready.</done>
</task>

<task type="auto">
  <name>Task 2: Player Page Integration</name>
  <files>client/src/app/player/[id]/page.tsx</files>
  <action>
    1. In the player page, fetch vocabulary from `/api/vocab/:id` on mount.
    2. Pass the fetched vocabulary to the `LyricList` component.
    3. Ensure loading states handle both lyrics and vocabulary fetching.
  </action>
  <verify>
    <automated>grep -q "api/vocab" client/src/app/player/\[id\]/page.tsx</automated>
  </verify>
  <done>Player page successfully fetches and displays vocabulary highlights.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive vocabulary highlighting and definition popovers.</what-built>
  <how-to-verify>
    1. Start the client and server.
    2. Navigate to a song player page.
    3. Verify that 5-10 words are highlighted with a dotted underline.
    4. Click a highlighted word and ensure a high-contrast popover appears with the definition and lemma.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> User | Rendering definitions and lyrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Cross-Site Scripting (XSS) | LyricList.tsx | mitigate | Use standard React string rendering (no dangerouslySetInnerHTML) for definitions and lyrics |
</threat_model>

<verification>
1. Verify Radix UI installation.
2. Manually verify the popover interaction and styling.
</verification>

<success_criteria>
User can see highlights and view definitions while the song is playing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-vocab-extraction/03-03-SUMMARY.md`
</output>
