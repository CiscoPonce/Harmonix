---
phase: 03-ai-vocab-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/db.js, server/package.json, server/services/aiService.js, server/utils/alignment.js, server/services/aiService.test.js, server/utils/alignment.test.js]
autonomous: true
requirements: [AI-01]
user_setup:
  - service: NVIDIA NIM
    why: "AI vocabulary extraction"
    env_vars:
      - name: NVIDIA_NIM_API_KEY
        source: "NVIDIA NIM Dashboard"
      - name: NVIDIA_NIM_BASE_URL
        source: "NVIDIA NIM Dashboard (usually https://integrate.api.nvidia.com/v1)"

must_haves:
  truths:
    - "Schema supports vocabulary items and song-specific mappings"
    - "User table supports proficiency level (CEFR)"
    - "System can communicate with NVIDIA NIM using OpenAI SDK"
    - "System can map AI-extracted words to specific lyric lines using Two-Pass Alignment"
  artifacts:
    - path: "server/services/aiService.js"
      provides: "AI extraction logic"
    - path: "server/utils/alignment.js"
      provides: "Word-to-line mapping algorithm"
  key_links:
    - from: "server/services/aiService.js"
      to: "NVIDIA NIM"
      via: "openai SDK"
---

<objective>
Establish the backend foundation for AI vocabulary extraction, including the database schema updates, AI service integration, and the word-mapping algorithm.

Purpose: To enable the system to identify and locate target vocabulary words within song lyrics using AI, tailored to user proficiency.
Output: Updated database schema, AI service client, and alignment utility.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-3/RESEARCH.md
@server/db.js
@server/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Test Scaffolds</name>
  <files>server/services/aiService.test.js, server/utils/alignment.test.js</files>
  <action>
    Create empty test files with basic Mocha/Chai imports to satisfy the automated verification requirements for subsequent tasks.
  </action>
  <verify>
    <automated>ls server/services/aiService.test.js server/utils/alignment.test.js</automated>
  </verify>
  <done>Test scaffolds created.</done>
</task>

<task type="auto">
  <name>Task 1: Schema Migration & SDK Setup</name>
  <files>server/db.js, server/package.json</files>
  <action>
    1. Install `openai` package in `server`.
    2. Update `server/db.js` to:
       - Add `cefr_level` column to `users` table (DEFAULT 'B1').
       - Create `vocab_items` table per RESEARCH.md.
       - Create `song_vocab_map` table with PK `(song_id, vocab_id, line_index, char_start)`. This allows multiple highlights of the same word (per checker feedback).
       - Create `user_vocab_progress` table for future SRS integration.
    Note: `song_id` will be the Deezer track ID.
  </action>
  <verify>
    <automated>npm --prefix server list openai && sqlite3 server/lyricword.db ".schema users" | grep cefr_level && sqlite3 server/lyricword.db ".schema song_vocab_map" | grep "PRIMARY KEY(song_id, vocab_id, line_index, char_start)"</automated>
  </verify>
  <done>Tables updated/created in SQLite and OpenAI SDK installed.</done>
</task>

<task type="auto">
  <name>Task 2: NVIDIA NIM Extraction Service</name>
  <files>server/services/aiService.js, server/services/aiService.test.js</files>
  <action>
    Create `server/services/aiService.js` that:
    1. Initializes `OpenAI` client using `NVIDIA_NIM_API_KEY` and `NVIDIA_NIM_BASE_URL`.
    2. Exports `extractVocabulary(lyricsText, targetLanguage, cefrLevel)` function.
    3. Implements the prompt pattern from RESEARCH.md (Persona: Professional Teacher).
    4. Requests JSON output with `word`, `lemma`, `definition`, `cefr_level`, and `reason`.
    5. Write a test in `server/services/aiService.test.js` to mock OpenAI and verify prompt construction.
  </action>
  <verify>
    <automated>npm --prefix server test server/services/aiService.test.js</automated>
  </verify>
  <done>AI service can generate extraction requests with correct prompt structure.</done>
</task>

<task type="auto">
  <name>Task 3: Two-Pass Alignment Implementation</name>
  <files>server/utils/alignment.js, server/utils/alignment.test.js</files>
  <action>
    Implement `server/utils/alignment.js` containing `mapVocabToLyrics(vocabItems, lyricsLines)`:
    1. **Pass 1 (Exact)**: Search for the extracted `word` within each line's `text`.
    2. **Pass 2 (Normalized)**: If not found, lowercase and strip punctuation from both `word` and `line.text` before searching.
    3. Return an array of all occurrences: `{ vocab_id, line_index, char_start }`.
    4. Write comprehensive tests in `server/utils/alignment.test.js` for exact and normalized matches, ensuring multiple occurrences in one line are handled.
  </action>
  <verify>
    <automated>npm --prefix server test server/utils/alignment.test.js</automated>
  </verify>
  <done>Alignment utility correctly identifies word locations in LRC-style lyric lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> NVIDIA NIM | API key and prompt data sent to external provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | server/services/aiService.js | mitigate | Store NVIDIA_NIM_API_KEY in .env, never commit |
| T-03-02 | Tampering | server/db.js | mitigate | Use parameterized queries for all DB interactions |
| T-03-06 | Elevation of Privilege | users.cefr_level | mitigate | Ensure CEFR level can only be updated via validated profile endpoints (Phase 3-03) |
</threat_model>

<verification>
1. Check that tables exist in SQLite with correct schemas.
2. Run unit tests for AI service and alignment utility.
</verification>

<success_criteria>
Backend can extract and map vocabulary words, supporting proficiency levels and multiple word occurrences.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-vocab-extraction/03-01-SUMMARY.md`
</output>
