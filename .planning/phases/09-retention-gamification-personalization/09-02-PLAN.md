---
phase: 09-retention-gamification-personalization
plan: 02
type: execute
wave: 2
depends_on: [09-01A-PLAN.md, 09-01B-PLAN.md]
files_modified: []
files_created: [client/src/app/onboarding/page.tsx, client/src/app/review/page.tsx, client/src/app/playlists/page.tsx, client/src/app/playlists/[id]/page.tsx, client/src/components/UndoDeleteToast.tsx]
autonomous: true
requirements: [STUDY-01]
user_setup:
  - service: None (frontend only — depends on Plan 1 backend)
env_vars: []
must_haves:
  truths:
    - "New users see a 2-step onboarding wizard at /onboarding with language selects"
    - "Users can skip onboarding with defaults (EN → ES)"
    - "Users can review due words via a dedicated SRS flashcard room at /review"
    - "SRS review shows word + translation simultaneously with 3 rating buttons"
    - "Users can end review early and save partial progress"
    - "All caught up state shows celebration animation"
    - "Users can create, view, and manage playlists at /playlists"
    - "Playlist deletion shows inline undo toast with 5s window"
    - "Empty states use UI-SPEC copy (Your collection is empty, All caught up!, etc.)"
  artifacts:
    - path: "client/src/app/onboarding/page.tsx"
      provides: "2-step language wizard with skip option"
    - path: "client/src/app/review/page.tsx"
      provides: "SRS flashcard review room"
    - path: "client/src/app/playlists/page.tsx"
      provides: "Playlist list with create + delete"
    - path: "client/src/app/playlists/[id]/page.tsx"
      provides: "Playlist detail with song list + remove"
    - path: "client/src/components/UndoDeleteToast.tsx"
      provides: "Inline undo delete toast component"
  key_links:
    - from: "client/src/app/onboarding/page.tsx"
      to: "server/routes/user.js"
      via: "PATCH /api/user/preferences"
    - from: "client/src/app/review/page.tsx"
      to: "server/routes/progress.js"
      via: "GET /api/progress/due + POST /api/progress/review"
    - from: "client/src/app/playlists/page.tsx"
      to: "server/routes/playlists.js"
      via: "GET /api/playlists + POST /api/playlists + DELETE /api/playlists/:id"
---

<objective>
Build all new frontend pages: onboarding wizard, SRS review room, and playlist management pages. These are standalone pages that consume the API routes from Plan 01A/01B. Each page follows the existing patterns from the codebase (dashboard card grid, auth context, apiFetch, Button variants, loading/error/empty states).

**Cross-plan dependency note:** The onboarding redirect for existing users without language preferences is handled in Plan 03 (Dashboard Integration). This plan creates the `/onboarding` page itself. The two plans are tightly coupled: any change to redirect logic must be mirrored in both Plan 02 (page accepts redirect arrivals) and Plan 03 Task 3 (redirect trigger).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Onboarding Page</name>
  <files>client/src/app/onboarding/page.tsx</files>
  <action>
  1. Create new page at `client/src/app/onboarding/page.tsx`
  2. Use 'use client' directive
  3. Full-page centered layout: `flex min-h-screen flex-col items-center justify-center bg-black px-4 text-white`
  4. 2-step wizard with useState for step, form values (native_language, target_language, genre, difficulty)
  5. Step indicator dots: `w-2 h-2 rounded-full` (bg-white for active, bg-zinc-700 for inactive)
  6. Step 1 — Language selection:
     - "Set your languages" heading (follows register page pattern)
     - Two select fields: Native Language + Target Language (empty by default, no pre-fill per D-09-05)
     - Language options: English, Spanish, French, German, Portuguese
     - "Continue" button (primary variant)
  7. Step 2 — Preferences (optional):
     - "Personalize your experience" heading
     - Genre multi-select (Any, Pop, Rock, Hip-Hop, Reggaeton)
     - Difficulty selector (Easy, Medium, Hard)
     - "Start learning" button (primary variant) — calls PATCH /api/user/preferences then router.push('/dashboard')
  8. "Skip → Use defaults" ghost button at bottom (calls PATCH /api/user/preferences with defaults: en→es, then redirects to /dashboard)
  9. Error display: `<p className="text-sm text-red-500">{error}</p>`
  10. Loading state: button disabled with spinner or "Saving..."
  </action>
  <done>Onboarding flow works end-to-end: languages persist, skip uses defaults, redirects to dashboard.</done>
  <verify>grep -q "onboarding" client/src/app/onboarding/page.tsx && echo "PASS" || echo "FAIL"</verify>
</task>

<task type="auto">
  <name>Task 2: SRS Review Room</name>
  <files>client/src/app/review/page.tsx</files>
  <action>
  1. Create new page at `client/src/app/review/page.tsx`
  2. Use 'use client' directive
  3. Auth check: redirect to /login if not authenticated
  4. States: loading, empty ("All caught up!" with CheckCircle2 animation), populated, error (banner), submitting
  5. Loading state: centered spinner (Loader2)
  6. Empty state: full centered layout with CheckCircle2 icon (w-16 h-16 text-green-500 animate-pulse), heading "All caught up!", body "No words due for review. Come back tomorrow.", "Back to Dashboard" link
  7. Data fetching: GET /api/progress/due?limit=20 on mount (useEffect + apiFetch with active flag)
  8. Card layout: centered single card max-w-md:
     - Progress header: "{currentIndex+1}/{dueWords.length}" badge (bg-zinc-100 dark:bg-zinc-900 text-xs font-bold uppercase tracking-widest)
     - Word: text-5xl font-black tracking-tighter uppercase italic text-center
     - Translation/definition: text-lg font-medium text-zinc-500 text-center
     - Lyric snippet if available: italic muted at bottom
  9. Rating buttons: horizontal row of 3 secondary variant Buttons:
     - "Good" → { is_correct: true, response_ms: 1200 }
     - "Hard" → { is_correct: true, response_ms: 3500 }
     - "Again" → { is_correct: false, response_ms: 5000 }
  10. "End review" ghost button below ratings — saves partial progress
  11. On last card or "End review": POST /api/progress/review with accumulated results, then router.push('/dashboard')
  12. Error state: inline red banner "Could not load review words. Try again."
  13. Submitting state: "End review" disabled while POST is in flight
  </action>
  <done>SRS review room works end-to-end: loads due words, shows cards, rates with SM-2 mapping, saves progress, handles empty state.</done>
  <verify>grep -q "progress/due" client/src/app/review/page.tsx && grep -q "progress/review" client/src/app/review/page.tsx && echo "PASS" || echo "FAIL"</verify>
</task>

<task type="auto">
  <name>Task 3: Playlist List Page</name>
  <files>client/src/app/playlists/page.tsx, client/src/components/UndoDeleteToast.tsx</files>
  <action>
  Page: client/src/app/playlists/page.tsx
  1. Use 'use client' directive
  2. Auth check
  3. Sticky nav with "Playlists" title, "Back" button to dashboard
  4. Create playlist form: Input (placeholder "Playlist name") + "Create" button
  5. Fetch playlists: GET /api/playlists on mount
  6. States: loading (skeleton), empty (UI-SPEC copy: "Your collection is empty" + "Explore songs" CTA), populated (list of playlist cards)
  7. Each playlist card shows: name, song count, delete button (trash icon or "Delete" text)
  8. Delete handler: DELETE /api/playlists/:id, then show UndoDeleteToast
  9. Click on playlist → navigate to /playlists/[id]
  
  Component: client/src/components/UndoDeleteToast.tsx
  10. Fixed bottom-center toast: "Playlist deleted." + "Undo" button
  11. 5s auto-dismiss via setTimeout
  12. On Undo: POST /api/playlists with the deleted name, toast shows "Playlist restored" briefly
  13. Restoring state: show spinner on Undo button while API call is in flight
  </action>
  <done>Playlist list page works: create, list, delete with undo toast. Follows UI-SPEC copy and patterns.</done>
  <verify>grep -q "playlists" client/src/app/playlists/page.tsx && echo "PASS" || echo "FAIL"</verify>
</task>

<task type="auto">
  <name>Task 4: Playlist Detail Page</name>
  <files>client/src/app/playlists/[id]/page.tsx</files>
  <action>
  1. Create new page at `client/src/app/playlists/[id]/page.tsx`
  2. Use 'use client' directive
  3. Use Next.js params pattern to get playlist ID
  4. Auth check
  5. Fetch playlist: GET /api/playlists/:id on mount
  6. States: loading (skeleton), empty songs ("No songs yet. Search and add songs from the player."), populated (song list), error
  7. Song list: each song row shows title + artist (link to /player/[song_id]) + "Remove" button
  8. Remove handler: DELETE /api/playlists/:id/songs/:songId, then refetch playlist
  </action>
  <done>Playlist detail page shows songs with remove functionality. Links to player for each song.</done>
  <verify>grep -q "playlists/" client/src/app/playlists/\[id\]/page.tsx 2>/dev/null && echo "PASS" || echo "FAIL"</verify>
</task>
</tasks>

<success_criteria>
1. /onboarding displays 2-step wizard, saves preferences, or skips to defaults
2. /review loads due words, allows rating each card, saves progress on end
3. /playlists lists user's playlists, creates new ones, deletes with undo
4. /playlists/[id] shows songs with remove functionality
5. All pages handle loading, empty, error states appropriately
6. All copy matches UI-SPEC.md exactly
7. Navigation flows work: /onboarding → /dashboard, /review → /dashboard, /playlists ↔ /dashboard
8. Next.js compiles without errors (check with npm run dev or next build)
</success_criteria>
