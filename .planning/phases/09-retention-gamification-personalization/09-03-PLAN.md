---
phase: 09-retention-gamification-personalization
plan: 03
type: execute
wave: 3
depends_on: [09-01A-PLAN.md, 09-01B-PLAN.md, 09-02-PLAN.md]
files_modified: [client/src/app/dashboard/page.tsx]
files_created: [client/src/components/BadgeGrid.tsx, client/src/components/BadgeUnlockToast.tsx, client/src/components/LanguageBadge.tsx, client/src/components/ReviewCountBadge.tsx]
autonomous: true
requirements: [STUDY-03]
user_setup:
  - service: None (frontend components — depends on Plan 1 API)
env_vars: []
must_haves:
  truths:
    - "Dashboard shows an Achievements card with badge grid (grayscale locked, color unlocked)"
    - "Badge unlock triggers a toast notification (4s auto-dismiss, cool-tone accent)"
    - "Dashboard nav shows language badge (EN → ES) next to email"
    - "Dashboard shows 'X words to review today' link to /review"
    - "Users without native_language set are redirected to /onboarding once per session"
    - "All copy matches UI-SPEC.md (No achievements yet, Start learning, etc.)"
  artifacts:
    - path: "client/src/components/BadgeGrid.tsx"
      provides: "Badge grid component with loading/empty/populated states"
    - path: "client/src/components/BadgeUnlockToast.tsx"
      provides: "Auto-dismiss toast for badge unlock notifications"
    - path: "client/src/components/LanguageBadge.tsx"
      provides: "Nav language indicator (EN → ES)"
    - path: "client/src/components/ReviewCountBadge.tsx"
      provides: "Compact 'X words to review today' link"
    - path: "client/src/app/dashboard/page.tsx"
      provides: "Updated dashboard with all new integrations"
  key_links:
    - from: "client/src/components/BadgeGrid.tsx"
      to: "server/routes/badges.js"
      via: "GET /api/badges"
    - from: "client/src/components/ReviewCountBadge.tsx"
      to: "server/routes/progress.js"
      via: "GET /api/progress/due?limit=1"
    - from: "client/src/app/dashboard/page.tsx"
      to: "client/src/app/onboarding/page.tsx"
      via: "router.push('/onboarding') when native_language is null"
---

<objective>
Integrate all Phase 9 features into the dashboard: badges card, playlists card, review count indicator, language badge in nav, and onboarding redirect. This plan adds the visual components that make the phase features visible and accessible from the main dashboard.

**Cross-plan dependency note:** Badge unlock toast must be triggered from both the dashboard (receives badges_unlocked from API responses) and from Plan 02's playlists page (playlist creation can unlock badges). Both pages render the same `BadgeUnlockToast` component and check `badges_unlocked` in API responses.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Badge Grid Component</name>
  <files>client/src/components/BadgeGrid.tsx</files>
  <action>
  1. Create new file `client/src/components/BadgeGrid.tsx`
  2. Use 'use client' directive
  3. Import lucide-react icons: Flame, BookOpen, Award, ListMusic, CalendarDays, Loader2
  4. BADGE_ICONS map: icon name string → lucide component (Flame, BookOpen, Award, ListMusic, CalendarDays)
  5. BADGE_COLORS map: category → Tailwind text color:
     - streak → text-purple-500
     - vocabulary → text-pink-500
     - quiz → text-blue-500
     - playlist → text-orange-500
     - daily_word → text-teal-500
  6. Fetch badges on mount: GET /api/badges
  7. Three states:
     - Loading: 3-column skeleton grid with 6 placeholder items (w-12 h-12 rounded-xl bg-zinc-900)
     - Empty: "No achievements yet" heading + "Complete lessons, build streaks, and explore songs to unlock badges." body + "Start learning" link to dashboard
     - Populated: 3-column grid of badge items
  8. Each badge item:
     - 48px icon container (w-12 h-12 rounded-xl)
     - Unlocked: bg-zinc-100 dark:bg-zinc-900 background, colored icon (category color), text-white label
     - Locked: bg-zinc-800/30 dark:bg-zinc-900/50 background, grayscale opacity-30 icon, text-zinc-700 label
     - Badge name below icon (text-[8px] font-bold uppercase tracking-widest)
  9. Error state: inline red text "Could not load achievements. Try again."
  </action>
  <done>BadgeGrid renders 3x2 grid with correct lock/unlock styling. Loading, empty, error states handled.</done>
</task>

<task type="auto">
  <name>Task 2: Badge Unlock Toast + Language Badge + Review Count</name>
  <files>client/src/components/BadgeUnlockToast.tsx, client/src/components/LanguageBadge.tsx, client/src/components/ReviewCountBadge.tsx</files>
  <action>
  A) BadgeUnlockToast.tsx:
    1. Fixed top-right toast: "fixed top-4 right-4 z-50"
    2. Styled container: rounded-xl border border-zinc-800 bg-black/90 backdrop-blur-xl p-4 shadow-2xl
    3. Props: badge { id, name } | null, onDismiss callback
    4. Auto-dismiss after 4s via useEffect + setTimeout
    5. Shows badge icon (colored per category) + "{badge.name} unlocked!" text
    6. Manual close X button

  B) LanguageBadge.tsx:
    1. Reads user from useAuth()
    2. Returns null if native_language or target_language is missing
    3. Format: "{native} → {target}" (uppercase, e.g., "EN → ES")
    4. Style: text-[10px] font-bold uppercase tracking-widest, px-2 py-1 rounded, border border-zinc-800 bg-black/50 text-zinc-300
    5. Clickable → links to /settings (placeholder for future phase)

  C) ReviewCountBadge.tsx:
    1. Fetches due word count via GET /api/progress/due?limit=1
    2. Loading: show Loader2 spinner
    3. Null/zero: return null (don't show if no words due)
    4. Positive: Link to /review showing "{count} words to review today →"
    5. Style: text-xs font-bold uppercase tracking-widest text-zinc-400 hover:text-white
  </action>
  <done>Three components created: badge toast auto-dismisses with correct icons/colors, language badge shows in nav, review count shows due word count.</done>
  <verify>grep -q "BadgeUnlockToast" client/src/components/BadgeUnlockToast.tsx 2>/dev/null && grep -q "LanguageBadge" client/src/components/LanguageBadge.tsx 2>/dev/null && grep -q "ReviewCountBadge" client/src/components/ReviewCountBadge.tsx 2>/dev/null && echo "PASS" || echo "FAIL"</verify>
</task>

<task type="auto">
  <name>Task 3: Dashboard Integration</name>
  <files>client/src/app/dashboard/page.tsx</files>
  <action>
  1. Add imports at top:
     - import { BadgeGrid } from '@/components/BadgeGrid'
     - import { LanguageBadge } from '@/components/LanguageBadge'
     - import { ReviewCountBadge } from '@/components/ReviewCountBadge'
     - import { BadgeUnlockToast } from '@/components/BadgeUnlockToast'
     - import { Award } from 'lucide-react' (add to existing lucide import)
  2. Add state for badge unlock toast: const [unlockedBadge, setUnlockedBadge] = useState<{id: string, name: string} | null>(null)
  3. Onboarding redirect: In the useEffect where user auth is checked (line 40-44), add after the existing redirect:
     - Check if user exists but has no native_language
     - Use sessionStorage key 'onboarding_redirected' to prevent re-redirect (per D-09-07)
     - If not redirected this session: set sessionStorage flag, then router.push('/onboarding')
  4. Nav section (around line 106): Insert <LanguageBadge /> before <ThemeToggle />
  5. Below hero section (after line 134 or before the "Or explore" section): Insert <ReviewCountBadge /> as a centered row
  6. Features grid (around line 145): Change sm:grid-cols-3 to sm:grid-cols-2 (2x2 grid better fits 4 cards)
  7. Add Achievements card as a new card in the grid (before closing </div> of the grid):
     - Icon box with Award icon
     - Heading: "Achievements" (font-black uppercase italic tracking-tighter text-xl)
     - Content: <BadgeGrid />
     - Style: identical to other cards (rounded-2xl border ... min-h-[250px])
  8. Add Playlists card as another card in the grid:
     - Icon box with ListMusic icon
     - Heading: "Playlists" (font-black uppercase italic tracking-tighter text-xl)
     - Fetch recent 3 playlists via GET /api/playlists?limit=3
     - Loading: skeleton with 3 rows
     - Empty: "Your collection is empty" + "Explore songs" link (per UI-SPEC)
     - Populated: list of playlist names with song count, last item is "View all →" link to /playlists
     - Style: same as other cards
  9. Badge unlock toast trigger: After any data fetch that returns badges_unlocked (progress/stats, playlists, etc.), check for new badges and setUnlockedBadge if present.
     - The dashboard checks the badges_unlocked array in relevant API responses
     - BadgeUnlockToast placed at top level of page div
     - <BadgeUnlockToast badge={unlockedBadge} onDismiss={() => setUnlockedBadge(null)} />
     - Note: the playlists page (Plan 02 Task 3) should also check badges_unlocked in POST /playlists responses and show a local BadgeUnlockToast
  </action>
  <done>Dashboard integrates all Phase 9 features: Achievements card, Playlists card, review count, language badge in nav, onboarding redirect. Badge toast triggers from API responses. All components visible and functional.</done>
  <verify>grep -q "BadgeGrid" client/src/app/dashboard/page.tsx && grep -q "LanguageBadge" client/src/app/dashboard/page.tsx && grep -q "ReviewCountBadge" client/src/app/dashboard/page.tsx && echo "PASS" || echo "FAIL"</verify>
</task>
</tasks>

<success_criteria>
1. Dashboard shows Achievements card with badge grid matching UI-SPEC
2. Locked badges appear grayscale, unlocked badges show in color
3. Language badge (EN → ES) appears in sticky nav next to email
4. "X words to review today →" shows when words are due
5. Users without language set are redirected to /onboarding once per session
6. Badge unlock toast appears and auto-dismisses after 4s
7. Dashboard layout adapts to 2x2 grid for 4 cards
8. All copy matches UI-SPEC.md exactly
</success_criteria>
