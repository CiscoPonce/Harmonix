---
phase: 08-harmonix-rebrand
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [client/src/app/page.tsx, client/src/app/dashboard/page.tsx, client/src/components/LandingHero.tsx, client/src/components/LandingFeatures.tsx, client/src/components/LandingTestimonials.tsx, client/src/components/LandingFooter.tsx, client/src/app/globals.css]
autonomous: true
requirements: [BRAND-02]
user_setup:
 - service: None
env_vars: []
must_haves:
 truths:
  - "Unauthenticated visitors see a public marketing landing page at '/'"
  - "Landing page has a hero section with logo, tagline, CTAs, and phone mockup"
  - "Landing page has a features section with 3 feature cards"
  - "Landing page has a testimonials section"
  - "Landing page has a footer with Support, Privacy, Terms links"
  - "Authenticated users are redirected to '/dashboard' which contains the existing study experience"
  - "Navigation has Sign In button for guests, dashboard link for logged-in users"
 artifacts:
  - path: "client/src/app/page.tsx"
    provides: "Public landing page with hero, features, testimonials, footer"
  - path: "client/src/app/dashboard/page.tsx"
    provides: "Authenticated dashboard (moved from old page.tsx)"
  - path: "client/src/components/LandingHero.tsx"
    provides: "Hero section component with logo, CTA buttons, phone mockup"
  - path: "client/src/components/LandingFeatures.tsx"
    provides: "Three feature cards: Melodic Recall, Rhythmic Pronunciation, Personalized Playlists"
  - path: "client/src/components/LandingTestimonials.tsx"
    provides: "User testimonial cards in a grid layout"
  - path: "client/src/components/LandingFooter.tsx"
    provides: "Footer with links and contact info"
 key_links:
  - from: "client/src/app/page.tsx"
    to: "client/src/components/LandingHero.tsx"
    via: "Landing page composition"
  - from: "client/src/app/page.tsx"
    to: "client/src/app/login/page.tsx"
    via: "Sign In CTA navigation"
  - from: "client/src/app/dashboard/page.tsx"
    to: "client/src/components/DailyWordCard.tsx"
    via: "Authenticated study experience"
---

<objective>
Build a public marketing landing page as the new root route ('/'), matching the Harmonix landing design reference. Move the existing authenticated dashboard experience to a new '/dashboard' route.

Purpose: Give unauthenticated visitors a professional product page that explains Harmonix and drives sign-ups, while preserving the full study dashboard for logged-in users.
Output: Landing page at '/', dashboard at '/dashboard', seamless auth-based routing.
</objective>

<tasks>
<task type="auto">
 <name>Task 1: Route Architecture — Move Dashboard</name>
 <files>client/src/app/page.tsx, client/src/app/dashboard/page.tsx</files>
 <action>
 Move the current authenticated home page content (DailyWordCard, SongSearch, Stats/Recent/Daily cards) from page.tsx into a new file at client/src/app/dashboard/page.tsx.
 Update the auth redirect: when a user is NOT logged in, they stay on '/' (landing page). When they ARE logged in and visit '/', redirect to '/dashboard'. The dashboard page should redirect unauthenticated users to '/login'.
 </action>
 <done>Dashboard lives at /dashboard, root route is free for the landing page.</done>
</task>
<task type="auto">
 <name>Task 2: Landing Page — Navigation Bar</name>
 <files>client/src/app/page.tsx</files>
 <action>
 Build a sticky top navigation bar with:
   - Left: Harmonix logo image (from /logo.png) + "HARMONIX" text + "LEARN WORDS THROUGH MUSIC" subtitle
   - Centre: Navigation links — Features, How It Works, Testimonials, About (smooth-scroll anchors)
   - Right: "Sign In" button linking to /login
 Style: light/white background, clean typography, border-bottom subtle shadow.
 </action>
 <done>Navigation bar renders with logo, links, and Sign In CTA.</done>
</task>
<task type="auto">
 <name>Task 3: Landing Page — Hero Section</name>
 <files>client/src/components/LandingHero.tsx</files>
 <action>
 Build the hero section matching the reference design:
   - Left side: Large Harmonix logo, headline "Master Vocabulary with Every Beat", subtext "Unlock language through rhythm and melody. Turn favourite songs into powerful learning tools.", two CTAs: "Get Started Free" (primary gradient button → /register) and "Watch How It Works" (text link).
   - Right side: Phone mockup showing the player UI with vocabulary popover overlays (use a styled div or generated screenshot).
   - Background: Subtle gradient blobs in orange-to-purple tones at corners.
 </action>
 <done>Hero section with logo, copy, CTAs, and phone mockup visual.</done>
</task>
<task type="auto">
 <name>Task 4: Landing Page — Features Section</name>
 <files>client/src/components/LandingFeatures.tsx</files>
 <action>
 Build a 3-column feature grid (responsive to single column on mobile):
   1. "Melodic Recall" — icon + "Associative learning binds definitions to music."
   2. "Rhythmic Pronunciation" — icon + "Perfect pronunciation with native speaker tracks."
   3. "Personalized Playlists" — icon + "AI adapts to your level and musical tastes."
 Each card has a colourful icon, bold title, and description. Matches the reference layout.
 </action>
 <done>Features section renders with 3 descriptive cards.</done>
</task>
<task type="auto">
 <name>Task 5: Landing Page — Testimonials Section</name>
 <files>client/src/components/LandingTestimonials.tsx</files>
 <action>
 Build a testimonial grid (3×2 on desktop, stacked on mobile) with placeholder user quotes:
   - "Learned 100 new words in a month!" — Alex R.
   - "The music context makes vocabulary stick!" — Maria S.
   - "Best language app I've ever used." — James T.
   - "I study Spanish while listening to Bad Bunny!" — Sofia L.
   - "My streak is at 30 days and counting." — David K.
   - "Finally, an app that makes learning fun." — Emma W.
 Each card: bordered, subtle shadow, quote text + attribution.
 </action>
 <done>Testimonials section renders with user quotes.</done>
</task>
<task type="auto">
 <name>Task 6: Landing Page — Footer</name>
 <files>client/src/components/LandingFooter.tsx</files>
 <action>
 Build a footer section with:
   - Left: Support | Privacy | Terms links (placeholder href="#")
   - Right: "Contact: info@harmonix.app" (or placeholder email)
   - Optional: "Download Now" badges for App Store and Google Play (as static images or placeholders for future use)
 Style: subtle border-top, muted text colours, consistent padding.
 </action>
 <done>Footer renders with navigation links and contact info.</done>
</task>
<task type="auto">
 <name>Task 7: Landing Page — CSS & Polish</name>
 <files>client/src/app/globals.css</files>
 <action>
 Add CSS custom properties and utility classes for the landing page:
   - Harmonix brand gradient (warm orange → pink → purple, sourced from the logo palette)
   - Gradient background blobs (decorative floating shapes using CSS radial-gradient + blur)
   - Smooth scroll behaviour for anchor navigation
   - Responsive breakpoints for hero and feature sections
   - Light-mode styles for the landing page (white background, dark text) contrasting with the dark-mode dashboard
 </action>
 <done>Landing page styles are polished, responsive, and visually premium.</done>
</task>
<task type="auto">
 <name>Task 8: Auth-Aware Navigation Updates</name>
 <files>client/src/app/page.tsx, client/src/app/dashboard/page.tsx, client/src/app/login/page.tsx</files>
 <action>
 Wire up the auth-aware routing:
   - page.tsx (landing): If user is authenticated, redirect to /dashboard.
   - dashboard/page.tsx: If user is NOT authenticated, redirect to /login.
   - login/page.tsx: After successful login, redirect to /dashboard (not /).
   - register/page.tsx: After registration + auto-login, redirect to /dashboard.
 Update any existing links/navigations that pointed to '/' to now point to '/dashboard' for internal app navigation (e.g. the logo click in the dashboard nav).
 </action>
 <done>Auth routing correctly separates public landing from private dashboard.</done>
</task>
</tasks>

<success_criteria>
1. Visiting '/' while logged out shows the full marketing landing page with hero, features, testimonials, and footer.
2. Clicking "Get Started Free" navigates to /register. Clicking "Sign In" navigates to /login.
3. After logging in, user lands on /dashboard with the full study experience (DailyWordCard, SongSearch, Stats, Recent, Daily cards).
4. Visiting '/' while logged in automatically redirects to /dashboard.
5. The landing page is responsive across mobile and desktop viewports.
6. The landing page uses a light theme; the dashboard retains the dark theme.
</success_criteria>
