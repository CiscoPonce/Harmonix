---
phase: 08-harmonix-rebrand
plan: 01
type: execute
wave: 1
depends_on: [07-daily-word]
files_modified: [client/src/app/layout.tsx, client/src/app/page.tsx, client/src/app/login/page.tsx, client/src/app/register/page.tsx, client/src/app/forgot-password/page.tsx, client/src/app/globals.css, client/src/app/favicon.ico, client/public/logo.png, server/package.json, server/db.js, run_env.sh, CLAUDE.md, README.md, project.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [BRAND-01]
user_setup:
 - service: None
env_vars: []
must_haves:
 truths:
  - "Every user-visible occurrence of 'LyricWord' is replaced with 'Harmonix'"
  - "The Harmonix logo (logoharmonix.png) is used as the app logo and favicon"
  - "Server package name and description reflect the Harmonix brand"
  - "Planning and documentation files reference Harmonix consistently"
  - "The app tagline reads 'Learn Words Through Music'"
 artifacts:
  - path: "client/public/logo.png"
    provides: "Harmonix logo asset served from /logo.png"
  - path: "client/src/app/favicon.ico"
    provides: "Harmonix favicon generated from logo"
  - path: "client/src/app/layout.tsx"
    provides: "Updated page title and meta description for Harmonix"
 key_links:
  - from: "client/src/app/page.tsx"
    to: "client/public/logo.png"
    via: "Logo image rendered in nav bar and loading screen"
  - from: "client/src/app/layout.tsx"
    to: "client/src/app/favicon.ico"
    via: "Browser tab icon"
---

<objective>
Rebrand the entire application from "LyricWord" to "Harmonix" across every layer: frontend UI text, server metadata, documentation, planning files, and deployment scripts.

Purpose: Unify the project under the new Harmonix identity ("Learn Words Through Music"), replacing all legacy brand references.
Output: A codebase where no user-visible or developer-visible reference to "LyricWord" remains.
</objective>

<tasks>
<task type="auto">
 <name>Task 1: Logo & Favicon Assets</name>
 <files>client/public/logo.png, client/src/app/favicon.ico, logoharmonix.png</files>
 <action>
 Copy logoharmonix.png into client/public/logo.png. Generate a favicon from the logo and replace client/src/app/favicon.ico. Ensure the logo is optimised for web (reasonable file size).
 </action>
 <done>Harmonix logo and favicon are in place in the client assets.</done>
</task>
<task type="auto">
 <name>Task 2: Frontend Brand Text Replacement</name>
 <files>client/src/app/page.tsx, client/src/app/layout.tsx, client/src/app/login/page.tsx, client/src/app/register/page.tsx, client/src/app/forgot-password/page.tsx</files>
 <action>
 Replace all occurrences of "LyricWord" with "Harmonix" in:
   - page.tsx: nav bar title, loading spinner text, footer copyright
   - layout.tsx: metadata title and description
   - login/page.tsx, register/page.tsx, forgot-password/page.tsx: any heading or branding text
 Update the nav bar to render the Harmonix logo image alongside the text.
 </action>
 <done>All frontend pages display "Harmonix" branding with logo.</done>
</task>
<task type="auto">
 <name>Task 3: Server & Config Metadata</name>
 <files>server/package.json, server/db.js, run_env.sh</files>
 <action>
 Rename server package from "lyric-server" to "harmonix-server" and update the description. Rename the database files from lyricword.db / lyricword.test.db to harmonix.db / harmonix.test.db in db.js (with a migration step to rename existing files on disk). Update run_env.sh comments and echo statements.
 </action>
 <done>Server identifies as harmonix-server, database files renamed.</done>
</task>
<task type="auto">
 <name>Task 4: Documentation & Planning Updates</name>
 <files>CLAUDE.md, README.md, project.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
 <action>
 Replace all "LyricWord" references with "Harmonix" in documentation and planning files. Update the roadmap with Phase 8 entry. Update STATE.md to reflect current phase.
 </action>
 <done>All documentation consistently uses the Harmonix brand.</done>
</task>
</tasks>

<success_criteria>
Running `grep -ri 'lyricword' ./ --include='*.tsx' --include='*.ts' --include='*.js' --include='*.json' --include='*.md' --include='*.sh' | grep -v node_modules | grep -v .next | grep -v .git` returns zero results. Every user-visible page shows "Harmonix" and the new logo.
</success_criteria>
