---
phase: 12-spotify-api-integration
plan: "11"
type: execute
wave: 8
depends_on:
  - "12-10"
files_modified: []
autonomous: false
requirements:
  - PHASE-12-MVP
  - D-12-01
  - D-12-02
  - D-12-03
  - D-12-04
  - D-12-05
  - D-12-06
  - D-12-07
  - D-12-08
  - D-12-09
  - D-12-10
  - D-12-11
  - D-12-12
  - D-12-13
  - D-12-14
  - D-12-15
must_haves:
  truths:
    - "Empty: fully empty Library, provider-empty, playlist-empty, and zero-match states use the specified copy and recovery."
    - "Loading: connection, Library groups, detail, and export use scoped skeleton/progress states without blocking loaded content."
    - "Error: OAuth, offline, rate-limit, expired-session, restricted, pre-create, and post-create failures have distinct safe recovery."
    - "Populated: provider-separated playlist cards, source-labelled rows, details, and reports work on web and Android."
    - "Partial: missing provider metadata and partial exports remain usable and are represented honestly."
    - "Overflow: responsive grids/lists and four-tab navigation avoid page-level horizontal scroll or inaccessible actions."
    - "Zero / one / many: cards, tracks, and report counts preserve layout and correct singular/plural through pagination."
    - "Long text: detail names wrap to two lines, cards/rows truncate accessibly, and narrow actions stack."
    - statement: "Responsive layout works at 375, 768, 1024, and 1440px with no overlap or horizontal page scroll."
      verification: backstop
    - statement: "Keyboard flow, 200% web zoom, large Flutter text scale, announcements, and contrast meet the UI contract."
      verification: backstop
    - statement: "Official Spotify asset use, source distinction, and API-provided deep links pass human policy review."
      verification: backstop
  artifacts:
    - path: ".planning/phases/12-spotify-api-integration/12-11-SUMMARY.md"
      provides: "Sandbox, device, UI, policy, branding, and quota release evidence"
  key_links:
    - from: "Real Spotify Development Mode app"
      to: "web and signed Android clients"
      via: "exact HTTPS callback and verified App Link"
      pattern: "spotify"
---

## Phase Goal

**As a** signed-in Harmonix learner, **I want to** connect my Spotify account from Settings and view and export playlists on web and Android, **so that** I can use my Spotify music inside Harmonix across both platforms.

<objective>
Run the mandatory real-service, physical-device, UI, policy, branding, and quota release gates.

Purpose: Validate facts that mocks/builds cannot prove and prevent Development Mode or unapproved policy assumptions from being presented as a public release.
Output: Recorded pass/fail evidence and an explicit sandbox-only or public-release go/no-go.
</objective>

<execution_context>
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/workflows/execute-plan.md
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spotify-api-integration/12-VALIDATION.md
@.planning/phases/12-spotify-api-integration/12-UI-SPEC.md
@.planning/phases/12-spotify-api-integration/COVERAGE.md
@.planning/phases/12-spotify-api-integration/12-10-SUMMARY.md
@docs/SPOTIFY-INTEGRATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate real Spotify journeys on web and signed Android</name>
  <read_first>
    - docs/SPOTIFY-INTEGRATION.md
    - .planning/phases/12-spotify-api-integration/12-VALIDATION.md
    - .planning/phases/12-spotify-api-integration/12-UI-SPEC.md
  </read_first>
  <files>None (verification evidence recorded in 12-11-SUMMARY.md)</files>
  <action>Execute the web and signed-Android sandbox journey exactly as listed, capture non-secret evidence for each step, and stop release on any OAuth binding, token custody, ownership, mutation, cleanup, rate, or log-redaction failure.</action>
  <what-built>Settings-first PKCE linking, backend token custody, provider-separated Library, normal/restricted detail, export/report, disconnect/reconnect, and Android verified return.</what-built>
  <how-to-verify>
    1. With an allowlisted Spotify Development Mode user, connect from web Settings; verify the exact HTTPS callback lands in Library, announces success, shows Harmonix immediately, and loads Spotify without exposing code/state/token in URL, storage, or logs.
    2. Repeat on a signed Android build from cold and warm states; verify the system browser returns through the verified App Link to Library once while ordinary launch defaults to Learn.
    3. Open an owned/collaborative playlist and a followed/restricted playlist on both clients; verify in-app tracks versus restriction copy and API-provided Open in Spotify.
    4. Export a real Harmonix playlist whose catalog outcomes can be observed without forcing provider faults; verify private destination, one source outcome per row, add success, report restoration after web refresh/Android restart, and Open in Spotify.
    5. Exercise feasible real conditions: OAuth cancellation/denial, ordinary provider/list/detail errors if naturally returned, offline behavior, restricted followed detail, and disconnect. Confirm disconnect immediately removes Spotify UI and persistence while Harmonix remains usable. Six-month expiry, deterministic 429 timing, zero-match classification, and controlled pre-create/post-create failures are accepted only from the fault-injected automated suites in plans 12-08 through 12-10, not claimed as sandbox observations.
  </how-to-verify>
  <acceptance_criteria>
    - "Web and Android complete every feasible OAuth/list/create/add/deep-link/cancel/disconnect path against the real sandbox."
    - "Fake-clock expiry, injected 429, zero-match, and controlled mutation failures have green automated evidence and are not represented as real-service tests."
    - "No high OAuth, token, ownership, mutation, cleanup, or log-redaction threat is observed."
    - "Spotify remains a data/deep-link provider; Harmonix adds no Spotify playback."
  </acceptance_criteria>
  <verify>
    <automated>cd server && NODE_ENV=test SPOTIFY_TOKEN_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= npm test && cd ../client && npm run test:spotify && npm run lint && npm run build && cd ../mobile && flutter test && flutter analyze</automated>
    <human-check>Complete the feasible real-service steps 1–5 with screenshots/log-redaction evidence; attach the already-green fault-injected automated evidence separately for expiry, 429, zero-match, and controlled provider failures.</human-check>
  </verify>
  <done>Every real-service happy/failure journey passes on web and Android with no observed high threat.</done>
  <resume-signal>Type "approved sandbox/device gate" or report the failing journey and observed state.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve responsive, accessible, attribution, and policy backstops</name>
  <read_first>
    - .planning/phases/12-spotify-api-integration/12-UI-SPEC.md
    - .planning/phases/12-spotify-api-integration/design/spotify-library-designer-reference.png
    - .planning/phases/12-spotify-api-integration/COVERAGE.md
  </read_first>
  <files>None (review evidence recorded in 12-11-SUMMARY.md)</files>
  <action>Perform the responsive, accessibility, branding, cache/deletion, mixed-provider, and no-AI review exactly as listed. Record named approval and evidence for each backstop; any policy rejection or accessibility blocker prevents release.</action>
  <what-built>Light/green provider-separated web and Flutter surfaces using official Spotify attribution and API-provided links.</what-built>
  <how-to-verify>
    1. Inspect web at 375, 768, 1024, and 1440px in light/dark themes; verify no overlap/horizontal page scroll and Library order/card rhythm follows the designer reference without its superseded Library Connect button.
    2. Verify keyboard order, visible focus, dialog trap/Escape/restoration, heading/status announcements, 200% zoom, reduced motion, and WCAG 2.2 AA contrast.
    3. Verify Flutter at large text scale in light/dark themes with 44px targets, four-tab order, no clipping, and persistent error/report content.
    4. Complete Spotify policy/legal review for mixed-provider sections, deterministic identity matching, cache expiry/deletion, official unmodified assets, source distinction, API-provided item links, the maximum 20-item Spotify shelf/detail sets, and the mandatory onward Spotify link after each set. Confirm no unsupported local continuation pagination was invented.
    5. Confirm no Spotify content reaches lyrics, learning, analytics-derived metrics, or NVIDIA NIM.
  </how-to-verify>
  <acceptance_criteria>
    - "All eight covered UI considerations and all three backstops pass on both platforms."
    - "Spotify branding/policy approval is explicit, not inferred."
    - "The provider groups remain visually separated and source-labelled."
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:spotify && npm run lint && npm run build && cd ../mobile && flutter test test/spotify_*_test.dart && flutter analyze</automated>
    <human-check>Record responsive screenshots, accessibility checks, and named policy/branding approval or rejection.</human-check>
  </verify>
  <done>All UI backstops and Spotify policy/branding conditions are explicitly approved.</done>
  <resume-signal>Type "approved UI/policy gate" or list each failed backstop/policy condition.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Make the Extended Quota public-release decision</name>
  <read_first>
    - docs/SPOTIFY-INTEGRATION.md
    - .planning/phases/12-spotify-api-integration/COVERAGE.md
    - .planning/phases/12-spotify-api-integration/12-01-SUMMARY.md
  </read_first>
  <files>None (release boundary recorded in 12-11-SUMMARY.md)</files>
  <decision>Choose the release boundary from factual Spotify quota status. Development Mode remains capped at five allowlisted users with a Premium owner; it cannot be labelled public.</decision>
  <action>Inspect the current Spotify quota evidence, select exactly one release boundary, and record it without upgrading pending/unknown status to approval.</action>
  <context>The implementation may be complete while Spotify Extended Quota is pending. This checkpoint separates product readiness from provider entitlement.</context>
  <options>
    <option id="public-release">
      <name>Public release</name>
      <pros>Available beyond five testers.</pros>
      <cons>Allowed only with documented Extended Quota approval and completed policy gates.</cons>
    </option>
    <option id="sandbox-release">
      <name>Five-user sandbox release</name>
      <pros>Allows controlled validation and demonstration.</pros>
      <cons>Must be labelled restricted and must not onboard non-allowlisted users.</cons>
    </option>
    <option id="hold">
      <name>Hold release</name>
      <pros>Prevents unsupported availability claims.</pros>
      <cons>Spotify integration remains unreleased until quota/policy blockers clear.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - "The chosen mode matches documented quota and policy approval."
    - "No production claim depends on a pending or unknown entitlement."
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/SPOTIFY-INTEGRATION.md','utf8'); if(!s.includes('Extended Quota')||!s.includes('five')) process.exit(1)"</automated>
  </verify>
  <done>The release is accurately classified as public, five-user sandbox, or held.</done>
  <resume-signal>Select public-release, sandbox-release, or hold and attach the quota-status evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Real Spotify sandbox → release evidence | External account/quota/policy behavior cannot be proven by mocks. |
| Human UI/policy review → release decision | Branding, accessibility, and entitlement require explicit sign-off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-47 | Spoofing | Real OAuth/App Link | high | mitigate | Web and signed-device sandbox verification of exact callback and verified return. |
| T-12-48 | Information Disclosure | Real logs/storage | high | mitigate | Inspect callback/provider logs, browser/device storage, and disconnect persistence during smoke. |
| T-12-49 | Elevation of Privilege | Public quota claim | high | mitigate | Blocking Extended Quota decision with attached evidence. |
| T-12-50 | Tampering | Branding/policy representation | high | mitigate | Blocking named human approval for assets, source separation, caching, matching, and no-AI use. |
| T-12-51 | Denial of Service | 429 behavior | high | mitigate | Fault-injected provider response and fake-sleep tests prove exact retry timing; sandbox checks only feasible visible recovery without claiming deterministic rate-limit induction. |
</threat_model>

## Multi-Source Coverage Audit

- **GOAL — Phase 12 MVP story:** covered by plans 12-04 through 12-11.
- **REQ — explicit ROADMAP IDs:** none mapped; `PHASE-12-MVP` is the plan-set trace key.
- **CONTEXT D-12-01–D-12-05:** prerequisites/tests/link/disconnect in 12-01, 12-02, 12-04, 12-05, 12-06, and 12-11.
- **CONTEXT D-12-06–D-12-10:** designer/provider Library, detail, and dual-platform UI in 12-03, 12-05 through 12-07, and 12-11.
- **CONTEXT D-12-11–D-12-15:** encrypted lifecycle, cache/matching, scopes, rates, export in 12-02, 12-04, and 12-08 through 12-11.
- **RESEARCH — exact HTTPS/PKCE/current endpoints/search 10/detail restriction/add 100:** covered by 12-01, 12-02, 12-04, 12-07, and 12-08.
- **RESEARCH — persisted user-scoped normalized `/me/playlists` metadata, complete-sync lifecycle, freshness/revalidation, direct restricted-detail ownership, and disconnect cleanup:** covered by 12-04, 12-07, 12-09, and 12-10.
- **RESEARCH — Premium five-user Development Mode/Extended Quota/six-month expiry:** covered by 12-01, 12-04, 12-10, and 12-11.
- **RESEARCH — policy/branding/cache/no AI:** covered by COVERAGE.md and plans 12-03, 12-05, and 12-08 through 12-11.
- **VALIDATION — ABI/backend/Flutter/web strategy/corpus:** covered by plans 12-02, 12-03, 12-09, and 12-10.
- **UI-SPEC — covered considerations/backstops:** implemented in 12-05 through 12-09 and verified above.
- **Deferred/out-of-scope exclusions:** Spotify import to learning, history personalization, automatic daily-word playlist, full-song playback, manual match override, and unsupported Spotify editing remain excluded.

<verification>
- All three blocking gates resolve with evidence.
- Any failed high-severity gate blocks release and returns to the owning plan.
</verification>

<success_criteria>
- Real web and Android journeys pass.
- UI/accessibility/branding/policy backstops pass.
- Release is explicitly public, five-user sandbox, or held based on quota evidence.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/12-spotify-api-integration/12-11-SUMMARY.md` — sandbox/device/UI/policy/quota evidence and release boundary; no production source symbols.

<output>
Create `.planning/phases/12-spotify-api-integration/12-11-SUMMARY.md` when done.
</output>
