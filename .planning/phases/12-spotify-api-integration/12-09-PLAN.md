---
phase: 12-spotify-api-integration
plan: "09"
type: execute
wave: 6
depends_on:
  - "12-08"
files_modified:
  - mobile/lib/services/api_client.dart
  - mobile/lib/screens/playlist_detail_screen.dart
  - mobile/lib/widgets/spotify_export_sheet.dart
  - mobile/lib/widgets/spotify_match_report.dart
  - mobile/test/spotify_export_test.dart
  - server/services/spotifyPlaylistList.test.js
  - server/routes/spotifyPlaylistList.test.js
  - server/services/spotifyPlaylistDetail.test.js
  - server/routes/spotifyPlaylistDetail.test.js
  - server/services/spotifyExportService.test.js
  - server/routes/spotifyExport.test.js
  - client/src/lib/spotifyContracts.test.ts
autonomous: true
requirements:
  - PHASE-12-MVP
  - D-12-05
  - D-12-08
  - D-12-10
  - D-12-13
  - D-12-15
must_haves:
  truths:
    - "Android exports the same owned, non-empty Harmonix playlist through the same backend job and displays the same D-12-13 factual report as web."
    - "Closing the Flutter sheet, recreating the route, or restarting the Android process does not cancel or orphan a running export; detail restores user-owned backend-confirmed progress/report through latest/by-id lookup."
    - "Zero-match, success, partial export, rate limit, offline, expired session, provider failure before creation, and failure after creation are visibly distinct."
    - "D-12-15 disables network-required sync/export while offline and honors backend retry timing without fake progress."
    - "D-12-05 disconnect and expired-session cleanup remove/obscure stale Spotify personal data on both clients."
    - "D-12-10 Material styling, text scaling, semantics, focus order, and four-tab navigation remain intact."
  artifacts:
    - path: "mobile/lib/widgets/spotify_export_sheet.dart"
      provides: "Native confirmation and real export stage progress"
    - path: "mobile/lib/widgets/spotify_match_report.dart"
      provides: "Native matched/unmatched/failed report and destination action"
    - path: "mobile/test/spotify_export_test.dart"
      provides: "Complete Android export and resilience state matrix"
  key_links:
    - from: "mobile/lib/widgets/spotify_export_sheet.dart"
      to: "POST /api/spotify/exports and GET /api/spotify/exports/:id"
      via: "ApiClient create and bounded status polling"
      pattern: "spotifyExport"
    - from: "mobile/lib/widgets/spotify_match_report.dart"
      to: "external_url"
      via: "validated API-provided Open in Spotify launcher"
      pattern: "externalUrl"
---

## Phase Goal

**As a** signed-in Harmonix learner, **I want to** connect my Spotify account from Settings and view and export playlists on web and Android, **so that** I can use my Spotify music inside Harmonix across both platforms.

<objective>
Complete Android export parity and close the required cross-platform failure-state matrix.

Purpose: Ensure the full Spotify journey remains factual and recoverable under offline, rate, expiry, restriction, partial-data, and partial-mutation conditions.
Output: Native export sheet/report plus adversarial regression tests across backend, web contracts, and Flutter.
</objective>

<execution_context>
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/workflows/execute-plan.md
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spotify-api-integration/12-08-SUMMARY.md
@.planning/phases/12-spotify-api-integration/12-UI-SPEC.md
@mobile/lib/screens/playlist_detail_screen.dart
@mobile/lib/services/api_client.dart
</context>

## Execution Scope Rationale

This 12-file Android export/resilience plan is irreducible because the native sheet/report must consume the durable export job while the same boundary proves cleanup and failure semantics across backend, web contracts, and Flutter. Atomic boundaries are Task 1 (Android export initiation/restoration/report, verified by `spotify_export_test.dart` plus analyze) and Task 2 (cross-platform resilience/cleanup audit, verified by the focused backend matrix, web contracts/lint/build, and all Spotify Flutter files). The second task changes tests only and does not reopen production ownership.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export from Flutter detail with recoverable progress and report</name>
  <read_first>
    - mobile/lib/screens/playlist_detail_screen.dart
    - mobile/lib/services/api_client.dart
    - mobile/test/spotify_export_test.dart
    - client/src/components/SpotifyExportDialog.tsx
    - client/src/components/SpotifyMatchReport.tsx
    - .planning/phases/12-spotify-api-integration/12-UI-SPEC.md
  </read_first>
  <files>mobile/lib/services/api_client.dart, mobile/lib/screens/playlist_detail_screen.dart, mobile/lib/widgets/spotify_export_sheet.dart, mobile/lib/widgets/spotify_match_report.dart, mobile/test/spotify_export_test.dart</files>
  <behavior>
    - "Export appears only for owned non-empty Harmonix detail when connected; empty, Spotify-source, and reconnect states are ineligible."
    - "Confirmation names the source/count/private destination and says matching occurs before creation."
    - "Polling renders backend current/total and matching/creating/adding stages; duplicate submission is disabled."
    - "Detail mount queries authenticated latest-by-source status and restores by job ID; widget-local state is not the job identity store."
    - "Report reconciles total/matched/unmatched/exported/failed and conditionally launches safe destination."
  </behavior>
  <action>Move `spotify_export_test.dart` from controlled RED to the normal green command in this task; list and detail tests are already green from plans 12-06 and 12-07. Add `startSpotifyExport(playlistId, idempotencyKey)`, `latestSpotifyExport(playlistId)`, and `spotifyExportStatus(jobId)` to `ApiClient`, preserving reason and retry timing. On every owned local detail mount, query the authenticated latest-by-source endpoint; when it returns a job, restore progress/report through the by-ID endpoint before enabling a new export. The backend's user-owned export row is the durable identity source, so sheet close, route disposal/recreation, and Android process restart recover without widget-local persistence. Build a Material `AlertDialog` or bottom sheet with Cancel initial action, `Start export`, real determinate progress, semantics/current-total labels, and no fake animation. Build a persistent native report matching web semantics: Needs review reasons, partial exported/matched/total counts, safe Open in Spotify only with a destination, Finish export, offline/expired/rate recovery. Respect mounted checks, reduced motion, 200% text, 44px targets, and prevent duplicate mutation. Do not expose cached-vs-searched implementation detail or permit manual match override.</action>
  <acceptance_criteria>
    - "All eligibility, progress, report, sheet close, route recreation, simulated process restart, zero, partial, and error-state widget tests pass."
    - "Backend route tests prove latest/by-id ownership and non-disclosing cross-user responses."
    - "Only API-confirmed progress is displayed."
    - "Open in Spotify rejects unsafe or missing URLs."
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter test test/spotify_export_test.dart && flutter analyze</automated>
  </verify>
  <done>Android users can complete and revisit the same factual export flow as web.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the complete resilience and cleanup matrix</name>
  <read_first>
    - server/services/spotifyExportService.test.js
    - server/services/spotifyPlaylistList.test.js
    - server/routes/spotifyPlaylistList.test.js
    - server/services/spotifyPlaylistDetail.test.js
    - server/routes/spotifyPlaylistDetail.test.js
    - server/routes/spotifyExport.test.js
    - client/src/lib/spotifyContracts.test.ts
    - mobile/test/spotify_settings_test.dart
    - mobile/test/spotify_library_list_test.dart
    - mobile/test/spotify_playlist_detail_test.dart
    - mobile/test/spotify_export_test.dart
    - .planning/phases/12-spotify-api-integration/12-VALIDATION.md
  </read_first>
  <files>server/services/spotifyPlaylistList.test.js, server/routes/spotifyPlaylistList.test.js, server/services/spotifyPlaylistDetail.test.js, server/routes/spotifyPlaylistDetail.test.js, server/services/spotifyExportService.test.js, server/routes/spotifyExport.test.js, client/src/lib/spotifyContracts.test.ts, mobile/test/spotify_library_list_test.dart, mobile/test/spotify_playlist_detail_test.dart, mobile/test/spotify_export_test.dart</files>
  <behavior>
    - "Disconnect cleanup, including user_spotify_playlists rows, offline, exact rate limit, six-month expired session, restricted followed detail, partial provider data, cross-market cache revalidation, zero match, partial export, pre-create failure, and post-create failure each have an assertion."
    - "Harmonix content remains usable under every Spotify read failure."
    - "Provider tokens, state, personal cache, and export jobs cannot be read after disconnect."
  </behavior>
  <action>Audit the named required scenarios against automated tests and add missing assertions at the owning boundary. Backend tests must seed two users with the same Spotify playlist provider ID, query SQLite after disconnect, and prove only the disconnected user's token/state/`user_spotify_playlists`/match-cache/job rows are removed while the other user's normalized playlist row remains; they must also prove an expired playlist row cannot authorize restricted detail without successful user-scoped revalidation. Fake clock must exercise six-month expiry; injected 429 plus fake sleep must assert exact Retry-After; injected provider failures must prove pre-create and controlled post-create outcomes; route tests must distinguish cross-user/not-found for latest and by-ID status. Add US/GB cross-market cache tests that force availability revalidation. Web contract and Flutter tests must assert distinct stable reason-to-copy/action mappings, preserve local content, and restore export jobs after client recreation. Add an explicit spy/fake proving no Spotify DTO, match candidate, playlist item, or report is passed to any OpenAI/NVIDIA NIM service. Keep provider failures redacted.</action>
  <acceptance_criteria>
    - "Every scenario named in the task behavior maps to at least one automated test."
    - "Cleanup assertions inspect every persistence store, including user-scoped normalized playlist rows, not only response status."
    - "No-AI policy has an executable negative integration assertion."
  </acceptance_criteria>
  <verify>
    <automated>cd server && NODE_ENV=test SPOTIFY_TOKEN_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= npx mocha services/spotifyService.test.js routes/spotify.test.js services/spotifyPlaylistList.test.js routes/spotifyPlaylistList.test.js services/spotifyPlaylistDetail.test.js routes/spotifyPlaylistDetail.test.js services/spotifyMatchService.test.js services/spotifyMatchCorpus.test.js services/spotifyExportService.test.js routes/spotifyExport.test.js && cd ../client && npm run test:spotify && npm run lint && npm run build && cd ../mobile && flutter test test/spotify_settings_test.dart test/spotify_deep_link_test.dart test/spotify_library_list_test.dart test/spotify_playlist_detail_test.dart test/spotify_export_test.dart</automated>
  </verify>
  <done>The phase's complete error, cleanup, and policy matrix is automated across all tiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Flutter mutation UI → export job | Repeated or stale client actions could duplicate provider side effects. |
| Persisted job → user report | Partial external outcomes must remain user-owned and factual. |
| Disconnect → residual stores/clients | Provider personal data could survive authorization removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-38 | Tampering | Duplicate mobile export | high | mitigate | Immediate UI disable plus backend idempotency key and ownership tests. |
| T-12-39 | Information Disclosure | Cross-user/stale report | high | mitigate | User-owned status query, disconnect deletion, expired-session obscuring, and DB assertions. |
| T-12-40 | Repudiation | Partial mutation | high | mitigate | Persist every stage/batch and render exact counts/destination state. |
| T-12-41 | Denial of Service | Poll/rate loops | medium | mitigate | Bounded polling, backend retry timing, stop on terminal/reconnect/offline states. |
| T-12-42 | Information Disclosure | Spotify content to AI | high | mitigate | No-AI integration spy and architecture boundary tests. |
</threat_model>

<verification>
- Backend Spotify suites, web test/lint/build, Flutter Spotify tests, and Flutter analyze pass.
- Required scenario audit has no missing automated row.
</verification>

<success_criteria>
- Android export is behaviorally equivalent to web.
- Every mandated failure state is distinct, tested, and honest.
- Disconnect and policy cleanup are proven at persistence/integration boundaries.
</success_criteria>

## Artifacts this phase produces

- `mobile/lib/widgets/spotify_export_sheet.dart` — `SpotifyExportSheet`.
- `mobile/lib/widgets/spotify_match_report.dart` — `SpotifyMatchReport`.
- `ApiClient.startSpotifyExport`, `ApiClient.spotifyExportStatus`.
- Expanded backend/web/mobile adversarial resilience tests.

<output>
Create `.planning/phases/12-spotify-api-integration/12-09-SUMMARY.md` when done.
</output>
