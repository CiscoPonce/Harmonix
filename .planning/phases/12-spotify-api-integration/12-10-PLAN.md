---
phase: 12-spotify-api-integration
plan: "10"
type: execute
wave: 7
depends_on:
  - "12-09"
files_modified:
  - docs/SPOTIFY-INTEGRATION.md
  - .planning/phases/12-spotify-api-integration/12-VALIDATION.md
autonomous: true
requirements:
  - PHASE-12-MVP
  - D-12-01
  - D-12-05
  - D-12-11
  - D-12-14
  - D-12-15
must_haves:
  truths:
    - "All backend, web, and Flutter automated gates pass from a clean dependency install/runtime."
    - "The runbook names exact February 2026 endpoints, scopes, callback, encryption rotation, six-month expiry, disconnect cleanup, rate behavior, five-user sandbox, and Extended Quota gate."
    - "COVERAGE.md has no unclassified Spotify capability and documents every OPT-OUT reason."
    - "12-VALIDATION.md is marked Nyquist compliant only after every referenced test exists and passes."
  artifacts:
    - path: "docs/SPOTIFY-INTEGRATION.md"
      provides: "Operator, sandbox, security, policy, troubleshooting, and release runbook"
    - path: ".planning/phases/12-spotify-api-integration/12-VALIDATION.md"
      provides: "Final automated evidence and remaining manual gate status"
  key_links:
    - from: "docs/SPOTIFY-INTEGRATION.md"
      to: ".planning/phases/12-spotify-api-integration/COVERAGE.md"
      via: "current endpoint/scope/policy matrix"
      pattern: "COVERAGE.md"
---

## Phase Goal

**As a** signed-in Harmonix learner, **I want to** connect my Spotify account from Settings and view and export playlists on web and Android, **so that** I can use my Spotify music inside Harmonix across both platforms.

<objective>
Create reproducible local release evidence and an exact Spotify operations runbook.

Purpose: Ensure the implementation can be tested, configured, rotated, cleaned up, and diagnosed without rediscovering Spotify constraints.
Output: Green local release matrix, finalized validation architecture, and operator documentation.
</objective>

<execution_context>
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/workflows/execute-plan.md
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spotify-api-integration/COVERAGE.md
@.planning/phases/12-spotify-api-integration/12-VALIDATION.md
@.planning/phases/12-spotify-api-integration/12-RESEARCH.md
@.planning/phases/12-spotify-api-integration/12-09-SUMMARY.md
@docs/RELEASES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and record the clean local release matrix</name>
  <read_first>
    - .planning/phases/12-spotify-api-integration/12-VALIDATION.md
    - server/package.json
    - client/package.json
    - mobile/pubspec.yaml
    - .planning/phases/12-spotify-api-integration/COVERAGE.md
  </read_first>
  <files>.planning/phases/12-spotify-api-integration/12-VALIDATION.md</files>
  <action>From the declared lockfiles/runtime, rebuild the active better-sqlite3 ABI, run the complete backend suite with a test-only 32-byte encryption key, web `test:spotify`/lint/build, Flutter full tests/analyze, and focused searches proving current endpoint names and no Spotify-to-AI integration. Update the validation map with actual plan/task IDs and green evidence. Set `wave_0_complete: true` and `nyquist_compliant: true` only when every Wave 0 file exists, every command is green, and no three consecutive implementation tasks lack automated verification. Do not fabricate counts or suppress command failures.</action>
  <acceptance_criteria>
    - "Every automated command exits zero."
    - "No legacy playlist endpoint, forced popularity match, provider token client storage, or Spotify-to-NIM call is present."
    - "Validation frontmatter reflects observed results only."
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm rebuild better-sqlite3 && NODE_ENV=test SPOTIFY_TOKEN_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= npm test && cd ../client && npm run test:spotify && npm run lint && npm run build && cd ../mobile && flutter test && flutter analyze</automated>
  </verify>
  <done>The complete local matrix is green and 12-VALIDATION.md contains factual evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Write the February 2026 operations and release runbook</name>
  <read_first>
    - .planning/phases/12-spotify-api-integration/COVERAGE.md
    - .planning/phases/12-spotify-api-integration/12-RESEARCH.md
    - .planning/phases/12-spotify-api-integration/12-01-SUMMARY.md
    - server/.env.example
    - docs/RELEASES.md
  </read_first>
  <files>docs/SPOTIFY-INTEGRATION.md</files>
  <action>Document exact environment validation, deployment-owned HTTPS callback and fixed Library/Settings App Link outcomes, assetlinks setup, Premium owner and five-user allowlist, D-12-14 scopes, current `/me/playlists`, the `user_spotify_playlists` composite user/provider schema and complete-sync-only upsert/prune lifecycle, playlist metadata expiry and user-scoped revalidation rules, direct restricted-detail ownership checks, `GET /playlists/{id}` metadata, `/playlists/{id}/items`, `/me/playlists` create, `/playlists/{id}/items` add, search limit 10, 100-item batches, owner/collaborator restriction, maximum 20 displayed Spotify items plus API-provided onward link, exact Retry-After handling, six-month refresh expiry, external versioned 32-byte encryption-key rotation with no fallback, transactional disconnect/deletion procedure including normalized playlist rows, source-and-market cache policy, latest/by-id export restoration, log redaction, no-AI rule, sandbox fixtures, web/Android smoke steps, Extended Quota release gate, and rollback/incident response. Clearly separate feasible real sandbox OAuth/list/create/add/deep-link checks from fake-clock expiry, injected 429, and controlled pre/post-create failure suites. Link COVERAGE.md and Spotify policy/design references. State that Development Mode is never a public-release substitute.</action>
  <acceptance_criteria>
    - "An operator can configure development and production without guessing a domain, secret, scope, or quota."
    - "Troubleshooting distinguishes offline, 429, invalid_grant, restricted detail, pre-create, and post-create failures."
    - "Security and deletion duties are concrete and test-linked."
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/SPOTIFY-INTEGRATION.md','utf8'); for(const x of ['/me/playlists','/playlists/{id}/items','Retry-After','six months','Extended Quota','NVIDIA NIM','100']) if(!s.includes(x)) throw new Error('missing '+x)"</automated>
  </verify>
  <done>The integration is operable and release constraints are explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator configuration → production runtime | Secrets, callback, signing, and quota facts control security posture. |
| Test evidence → release decision | Incomplete or suppressed checks could falsely authorize release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-43 | Repudiation | Validation evidence | high | mitigate | Run unsuppressed clean commands and update validation only from observed results. |
| T-12-44 | Information Disclosure | Runbook secrets/logging | high | mitigate | Document names/sources/rotation and redaction, never real values. |
| T-12-45 | Elevation of Privilege | Quota/release configuration | high | mitigate | Separate five-user sandbox from Extended Quota release approval. |
| T-12-46 | Tampering | Stale API documentation | medium | mitigate | Pin runbook to February 2026 endpoints and link the coverage matrix/current official references. |
</threat_model>

<verification>
- Full automated release matrix passes.
- Validation status is factual.
- Runbook contains every required current constraint and recovery path.
</verification>

<success_criteria>
- Local implementation quality is release-candidate ready.
- Manual external/policy/UI gates are the only remaining checks.
</success_criteria>

## Artifacts this phase produces

- `docs/SPOTIFY-INTEGRATION.md`.
- Updated `.planning/phases/12-spotify-api-integration/12-VALIDATION.md` with plan mapping and observed automated evidence.

<output>
Create `.planning/phases/12-spotify-api-integration/12-10-SUMMARY.md` when done.
</output>
