---
phase: 12-spotify-api-integration
plan: "05"
type: execute
wave: 2
depends_on:
  - "12-03"
  - "12-04"
files_modified:
  - client/src/app/settings/page.tsx
  - client/src/components/SpotifyConnectionCard.tsx
  - client/src/app/playlists/page.tsx
  - client/src/lib/api.ts
  - client/src/lib/spotifyContracts.ts
  - client/public/spotify-logo.svg
autonomous: true
requirements:
  - PHASE-12-MVP
  - D-12-02
  - D-12-03
  - D-12-04
  - D-12-05
  - D-12-06
  - D-12-07
  - D-12-08
must_haves:
  truths:
    - "A signed-in web user starts Spotify linking only from the D-12-02/D-12-03 Settings card and successful D-12-04 completion lands on Library."
    - "The Settings card renders Connect, Connected, Reconnect, Disconnect, connecting, disconnecting, and safe provider-error states."
    - "D-12-05 disconnect waits for backend acknowledgement before removing provider UI."
    - "D-12-06 Library order is Harmonix Playlists, Spotify Playlists, Recent Discoveries; failures in Spotify never hide local content."
    - "D-12-07 Library has no primary Connect Spotify action; recovery links to Settings."
    - "D-12-08 provider labels and stable IDs prevent local/Spotify namespace collisions."
  artifacts:
    - path: "client/src/components/SpotifyConnectionCard.tsx"
      provides: "Settings-owned web connection state machine"
    - path: "client/src/app/playlists/page.tsx"
      provides: "Provider-separated, independently settled web Library"
    - path: "client/public/spotify-logo.svg"
      provides: "Current official unmodified provider mark"
  key_links:
    - from: "SpotifyConnectionCard"
      to: "POST /api/spotify/auth/start"
      via: "apiFetch and validated authorization URL navigation"
      pattern: "/spotify/auth/start"
    - from: "client/src/app/playlists/page.tsx"
      to: "GET /api/spotify/playlists"
      via: "independent request preserving Harmonix data"
      pattern: "/spotify/playlists"
---

## Phase Goal

**As a** signed-in Harmonix learner, **I want to** connect my Spotify account from Settings and view and export playlists on web and Android, **so that** I can use my Spotify music inside Harmonix across both platforms.

<objective>
Complete the browser-visible Settings connection → callback → provider-separated Library journey over the backend seam from 12-04.

Purpose: Make the highest-priority web journey usable without moving token custody or provider policy into Next.js.
Output: Web Settings account management, callback recovery, and an honest mixed-provider Library consumer.
</objective>

<execution_context>
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/workflows/execute-plan.md
@/home/cisco/Documents/Oracle server/lyrics/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spotify-api-integration/12-03-SUMMARY.md
@.planning/phases/12-spotify-api-integration/12-04-SUMMARY.md
@.planning/phases/12-spotify-api-integration/12-UI-SPEC.md
@client/src/app/playlists/page.tsx
@client/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the web Settings connection and recovery journey</name>
  <read_first>
    - client/AGENTS.md
    - client/src/lib/api.ts
    - client/src/app/dashboard/page.tsx
    - client/src/lib/spotifyContracts.ts
    - .planning/phases/12-spotify-api-integration/12-UI-SPEC.md
  </read_first>
  <files>client/src/app/settings/page.tsx, client/src/components/SpotifyConnectionCard.tsx, client/src/lib/api.ts, client/src/lib/spotifyContracts.ts, client/public/spotify-logo.svg</files>
  <behavior>
    - "The connection card sits directly below profile and represents every stable backend state."
    - "Connect starts through authenticated Express and navigates only to a validated accounts.spotify.com URL."
    - "Success is announced in Library; cancellation/error returns to Settings with an allowlisted recovery state."
  </behavior>
  <action>Follow the installed Next 16.2.9 documentation rule from `client/AGENTS.md`. Obtain the current official Spotify mark, preserve it unmodified, and record source/date for release review. Create `/settings` and the connection card directly below profile with D-12-03 states, disabled duplicate mutations, and destructive disconnect confirmation. Start auth via `apiFetch`; validate the returned authorization host before browser navigation. Render only fixed non-secret callback outcomes and never parse or store provider code, state, or tokens. Remove rendered provider state only after backend disconnect succeeds.</action>
  <verify>
    <automated>cd client && npm run test:spotify && npm run lint && npm run build</automated>
  </verify>
  <done>A signed-in web user can connect, recover, reconnect, and disconnect exclusively through Settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the provider-separated Library API without hiding Harmonix</name>
  <read_first>
    - client/src/app/playlists/page.tsx
    - client/src/lib/spotifyContracts.ts
    - .planning/phases/12-spotify-api-integration/12-UI-SPEC.md
    - .planning/phases/12-spotify-api-integration/design/spotify-library-designer-reference.png
  </read_first>
  <files>client/src/app/playlists/page.tsx, client/src/lib/spotifyContracts.ts</files>
  <behavior>
    - "Harmonix and Spotify requests settle independently and Recent Discoveries follows both playlist groups."
    - "At most 20 Spotify cards render, followed only by an API-provided safe onward Spotify link."
    - "Disconnected, loading, empty, offline, rate, reconnect, and provider-error states preserve local Library behavior."
  </behavior>
  <action>Implement the light/green, dark-compatible responsive composition with create action, `HARMONIX PLAYLISTS`, `SPOTIFY PLAYLISTS`, then `RECENT DISCOVERIES`. Fetch local and provider data independently so provider failure cannot clear Harmonix playlists or recent discoveries. Render scoped skeletons and factual empty/offline/rate/reconnect/error recovery. Display no more than 20 attributed Spotify cards, use provider-aware stable keys and `/playlists/spotify/{encoded provider_id}`, and show an onward Spotify action only from a validated API URL. Preserve local create/delete/navigation and Deezer preview behavior. Do not add a Library Connect CTA, provider playback, local continuation pagination, or AI/NIM transfer.</action>
  <verify>
    <automated>cd client && npm run test:spotify && npm run lint && npm run build</automated>
  </verify>
  <done>The browser-visible callback journey ends in an honest, usable, provider-separated Library.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser Settings → authenticated Express | User initiates provider authorization and deletion. |
| Backend DTO → Next router/DOM | Untrusted provider text, IDs, and URLs reach rendering/navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-21 | Spoofing | Authorization navigation | high | mitigate | Validate the backend-returned accounts.spotify.com URL and accept no caller destination. |
| T-12-22 | Information Disclosure | Browser storage/logs | high | mitigate | Provider credentials remain backend-only; clients consume safe status/DTO fields. |
| T-12-23 | Tampering | Provider identity/display | high | mitigate | Provider enum, stable IDs, encoded paths, plain-text rendering, and safe URL parser tests. |
| T-12-24 | Information Disclosure | Disconnect residue | high | mitigate | Remove UI only after backend transactional cleanup acknowledgement. |
</threat_model>

<verification>
- Each task runs web contract tests, lint, and production build.
- Plan 12-10 performs real browser callback, layout, focus, and scoped-state acceptance.
</verification>

<success_criteria>
- The complete web Settings → callback → Library journey is usable.
- Settings remains the only primary connection surface.
- Local Library content remains usable under every Spotify state.
</success_criteria>

<output>
Create `.planning/phases/12-spotify-api-integration/12-05-SUMMARY.md` when done.
</output>
