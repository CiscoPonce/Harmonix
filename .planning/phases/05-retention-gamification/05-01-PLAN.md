---
phase: 05-retention-gamification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/db.js, server/routes/progress.js, server/routes/progress.test.js, server/services/srsEngine.js, server/services/srsEngine.test.js]
autonomous: true
requirements: [STUDY-01, STUDY-03]
user_setup:
 - service: None
env_vars: []
must_haves:
 truths:
  - "System tracks daily streaks and XP in user_vocab_progress"
  - "SRS engine calculates next review date based on performance"
  - "API exposes progress/stats endpoints"
 artifacts:
  - path: "server/services/srsEngine.js"
   provides: "Spaced Repetition algorithm"
  - path: "server/routes/progress.js"
   provides: "Progress and gamification API"
 key_links:
  - from: "server/services/srsEngine.js"
   to: "server/db.js"
   via: "data access"
---

<objective>
Implement the Spaced Repetition System (SRS) and gamification tracking to motivate consistent practice.

Purpose: To help users retain vocabulary through scientifically spaced reviews and progress tracking.
Output: SRS engine and progress API endpoints.
</objective>

<context>
@.planning/phases/04-active-learning/04-01-SUMMARY.md
@server/db.js
</context>

<tasks>

<task type="auto">
 <name>Task 0: Create Test Scaffolds</name>
 <files>server/services/srsEngine.test.js, server/routes/progress.test.js</files>
 <action>
 Create empty test files with basic Mocha/Chai imports.
 </action>
 <verify>
 <automated>ls server/services/srsEngine.test.js server/routes/progress.test.js</automated>
 </verify>
 <done>Test scaffolds created.</done>
</task>

<task type="auto">
 <name>Task 1: User Stats Schema Extension</name>
 <files>server/db.js</files>
 <action>
 Extend `users` table or create `user_stats` table with:
 - `user_id` (FK)
 - `streak_days` INTEGER DEFAULT 0
 - `total_xp` INTEGER DEFAULT 0
 - `last_study_date` DATE
 - `daily_goal` INTEGER DEFAULT 20 (words)
 Add indexes on user_id for performance.
 </action>
 <verify>
 <automated>sqlite3 server/lyricword.db ".schema user_stats" | grep "streak_days" && sqlite3 server/lyricword.db ".schema users" | grep "cefr_level"</automated>
 </verify>
 <done>Stats schema added.</done>
</task>

<task type="auto">
 <name>Task 2: SRS Engine Implementation</name>
 <files>server/services/srsEngine.js, server/services/srsEngine.test.js</files>
 <action>
 Implement `server/services/srsEngine.js`:
 1. `calculateNextReview(stability, difficulty, performance)` → returns { next_interval, new_stability, new_difficulty }
 2. Simplified FSRS-inspired algorithm (SM-2 variant):
    - Correct: increase stability, increase interval (multiply by 1.5-2.5x)
    - Incorrect: reset or shorten interval, increase difficulty
 3. Write unit tests covering various performance scenarios.
 </action>
 <verify>
 <automated>npm --prefix server test server/services/srsEngine.test.js</automated>
 </verify>
 <done>SRS engine calculates review schedules.</done>
</task>

<task type="auto">
 <name>Task 3: Progress API Routes</name>
 <files>server/routes/progress.js, server/routes/progress.test.js, server/index.js</files>
 <action>
 1. Create `server/routes/progress.js`:
    - GET /api/progress/stats → returns streak, xp, daily progress
    - POST /api/progress/review → updates SRS schedule after quiz completion (batch update)
    - GET /api/progress/due → returns vocab_ids due for review today
 2. All protected by `authenticateToken`.
 3. Mount in `server/index.js` at `/api/progress`.
 4. Write tests for stats and review updates.
 </action>
 <verify>
 <automated>npm --prefix server test server/routes/progress.test.js</automated>
 </verify>
 <done>Progress API is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Study progress and stats submissions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | user_stats | mitigate | Parameterized DB queries |
| T-05-02 | Elevation of Privilege | /api/progress/* | mitigate | JWT auth required |
</threat_model>

<verification>
1. Run unit tests.
2. Test /api/progress/stats with a valid token.
</verification>

<success_criteria>
Frontend can fetch user stats (streak, XP) and update review schedules after quizzes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retention-gamification/05-01-SUMMARY.md`
</output>
