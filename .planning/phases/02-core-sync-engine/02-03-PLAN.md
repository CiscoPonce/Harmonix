---
phase: 02-core-sync-engine
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [client/src/components/LyricList.tsx, client/src/components/Player.tsx, client/src/app/player/[id]/page.tsx]
autonomous: false
requirements: [PLAYER-01, PLAYER-02]
must_haves:
  truths:
    - "User can see lyrics highlighting in sync with audio"
    - "User can click a lyric line to jump the audio to that position"
    - "High-contrast minimalist UI is maintained"
  artifacts:
    - path: "client/src/components/LyricList.tsx"
      provides: "Scrollable interactive lyric display"
    - path: "client/src/components/Player.tsx"
      provides: "Audio controls and sync integration"
    - path: "client/src/app/player/[id]/page.tsx"
      provides: "Main playback experience"
  key_links:
    - from: "player/[id]/page.tsx"
      to: "useSyncEngine"
      via: "hook usage"
---

<objective>
Build the interactive "Karaoke" UI that displays synced lyrics and allows users to jump to specific timestamps by clicking on lines.

Purpose: Delivering the core learning experience of the product.
Output: Integrated player page and interactive lyric components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-2/RESEARCH.md
@client/src/hooks/useSyncEngine.ts
@client/src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Interactive LyricList Component</name>
  <files>client/src/components/LyricList.tsx</files>
  <action>
    - Create `LyricList` component that takes `lines`, `currentLineIndex`, and `onLineClick`.
    - Map over `lines` and render each line.
    - Highlight the `currentLineIndex` using a distinct high-contrast style (e.g., white text vs gray for inactive).
    - Implement `onClick` for each line that calls `onLineClick(line.timeSeconds)`.
  </action>
  <verify>
    <automated>grep -q "onClick" client/src/components/LyricList.tsx</automated>
  </verify>
  <done>LyricList renders lines and supports interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Build Player Page Integration</name>
  <files>client/src/app/player/[id]/page.tsx</files>
  <action>
    - Create a dynamic route `/player/[id]`.
    - Fetch track metadata (from `/api/tracks/:id`) and lyrics (from `/api/lyrics`) on mount.
    - Initialize an `<audio>` element with the preview URL.
    - Use the `useSyncEngine` hook to wire everything together.
    - Render `LyricList` and basic play/pause controls.
  </action>
  <verify>
    <automated>ls client/src/app/player/\[id\]/page.tsx</automated>
  </verify>
  <done>Player page exists and integrates the sync engine with UI components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The core "Karaoke" sync engine and interactive lyric UI.</what-built>
  <how-to-verify>
    1. Start the server: `cd server && npm start`
    2. Start the client: `cd client && npm run dev`
    3. Visit `http://localhost:3000/player/SOME_DEEZER_ID` (Find an ID like `132143` for Daft Punk - One More Time).
    4. Play the audio.
    5. Observe: Lyrics should highlight in sync with the audio.
    6. Action: Click a lyric line.
    7. Observe: Audio should jump to that line and stay in sync.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Audio | Interaction with browser audio APIs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Player Page | mitigate | Ensure track IDs are validated and only metadata is exposed. |
</threat_model>

<verification>
Human verification of sync quality and interaction responsiveness.
</verification>

<success_criteria>
Users can listen to a 30s preview with perfectly synced lyric highlighting and can jump to any line by clicking it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sync-engine/02-03-SUMMARY.md`
</output>
