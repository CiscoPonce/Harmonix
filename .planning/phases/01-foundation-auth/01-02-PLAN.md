---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [client/package.json, client/tailwind.config.ts, client/app/globals.css, client/app/layout.tsx, client/components/ui/Button.tsx, client/components/ui/Input.tsx]
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "Application loads with a pure black background (#000000)"
    - "Text is high-contrast white or zinc-400"
    - "No 'flash of unstyled content' (FOUC) when switching themes or loading"
  artifacts:
    - path: "client/tailwind.config.ts"
      provides: "Custom palette and dark mode configuration"
    - path: "client/app/layout.tsx"
      provides: "ThemeProvider and global layout wrapper"
    - path: "client/components/ui/Button.tsx"
      provides: "High-contrast minimalist button component"
  key_links:
    - from: "client/app/layout.tsx"
      to: "next-themes"
      via: "ThemeProvider"
---

<objective>
Setup the Next.js frontend with Tailwind CSS and implement the high-contrast minimalist dark theme foundation.

Purpose: Deliver the distraction-free UI environment required for focused learning.
Output: A styled Next.js scaffold with a high-contrast design system and base components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Frontend & Theme System</name>
  <files>client/package.json, client/tailwind.config.ts, client/app/layout.tsx, client/app/globals.css</files>
  <action>
    - Initialize Next.js project in `client/` using `npx create-next-app@latest`.
    - Install `next-themes`, `lucide-react`, `tailwind-merge`, `clsx`.
    - Configure `client/tailwind.config.ts`:
      - Set `darkMode: 'class'`.
      - Define colors: `black: '#000000'`, `zinc: { 900: '#18181b', 800: '#27272a', 400: '#a1a1aa' }`.
    - Update `client/app/globals.css`:
      - Set `:root` and `.dark` variables for high contrast.
      - Apply `bg-black text-white` to `body`.
    - Update `client/app/layout.tsx`:
      - Wrap `children` with `ThemeProvider` (attribute="class", defaultTheme="dark").
      - Add `suppressHydrationWarning` to `html` tag.
  </action>
  <verify>
    <automated>
      grep -q "bg-black" client/app/globals.css && \
      grep -q "ThemeProvider" client/app/layout.tsx && \
      grep -q "suppressHydrationWarning" client/app/layout.tsx
    </automated>
  </verify>
  <done>Frontend initialized with high-contrast dark theme as default.</done>
</task>

<task type="auto">
  <name>Task 2: Create Minimalist Base Components</name>
  <files>client/components/ui/Button.tsx, client/components/ui/Input.tsx</files>
  <action>
    - Create `Button.tsx`: High-contrast variants (primary: white text/black bg/white border, secondary: zinc borders).
    - Create `Input.tsx`: Minimalist styling, zinc-800 borders, high-contrast focus rings.
    - Use `lucide-react` for any necessary icons.
  </action>
  <verify>
    <automated>
      test -f client/components/ui/Button.tsx && \
      test -f client/components/ui/Input.tsx
    </automated>
  </verify>
  <done>Base UI components created following the minimalist design tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | User interacts with theme toggles (if any) and components. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | client/layout.tsx | accept | Minimal client-side JS for theme management; low risk. |
</threat_model>

<verification>
- View application in browser; confirm background is #000000.
- Check text contrast against white/zinc.
- Verify components render with correct high-contrast borders.
</verification>

<success_criteria>
- Distraction-free minimalist dark interface is active.
- Tailwind and next-themes are correctly configured.
- Base components are ready for the Auth UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
