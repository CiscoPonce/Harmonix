---
phase: 17
plan: 17-01
title: "Keystore and signed Play AAB"
depends_on: []
autonomous: false
---

# Plan 17-01: Keystore + signed AAB

Human must create the keystore (passwords). Do not commit `.jks` or `key.properties`.

**Checklist:** [17-CHECKLIST.md](./17-CHECKLIST.md) section A.

## Objective

Produce `mobile/build/app/outputs/bundle/release/app-release.aab` signed with the upload key, pointed at `https://harmonix.peeporunclub.co.uk/api`.

## Environment on this PC

```bash
export JAVA_HOME="$HOME/.local/jdk/jdk-17"
export PATH="$JAVA_HOME/bin:$HOME/flutter/bin:$PATH"
```

## Acceptance

- `upload-keystore.jks` + `key.properties` exist under `mobile/android/` and are gitignored
- `flutter build appbundle --release --dart-define=API_BASE=https://harmonix.peeporunclub.co.uk/api` succeeds
- Version in the bundle is `1.0.8` / `11`
