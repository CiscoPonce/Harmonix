---
phase: 11-word-phonics-tts
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - client/src/components/DailyWordCard.tsx
autonomous: false
requirements: ["PHONICS-01"]

must_haves:
  truths:
    - "Speaker icon appears next to phonics representation for supported languages (D-11-06, D-11-16)"
    - "Clicking speaker icon plays pronunciation audio (D-11-13)"
    - "Icon pulses during playback, returns to normal when finished (D-11-14, D-11-15)"
    - "Icon hidden for unsupported languages (D-11-06)"
    - "Error toast shows 'Pronunciation unavailable' after retry failure (D-11-18, D-11-19)"
    - "Uses Lucide Volume2 icon (D-11-17)"
  artifacts:
    - path: "client/src/components/DailyWordCard.tsx"
      provides: "Pronunciation button with audio playback, pulsing animation, error handling"
  key_links:
    - from: "client/src/components/DailyWordCard.tsx"
      to: "/api/daily-word/pronounce"
      via: "apiFetch GET with word query param"
      pattern: "apiFetch.*daily-word/pronounce"
    - from: "client/src/components/DailyWordCard.tsx"
      to: "user.target_language"
      via: "useAuth hook"
      pattern: "user\\?\\.target_language"
---

<objective>
Add the pronunciation speaker button to DailyWordCard.tsx with on-demand audio playback, pulsing animation during playback, language-gated visibility, retry logic, and error toast.

Purpose: Complete the frontend half of the TTS feature — users click a speaker icon next to the phonics display to hear word pronunciation.
Output: DailyWordCard renders a Volume2 icon button for supported languages that plays pronunciation audio via Blob URL.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-word-phonics-tts/11-CONTEXT.md
@.planning/phases/11-word-phonics-tts/11-PATTERNS.md
@.planning/phases/11-word-phonics-tts/11-01-SUMMARY.md
@client/src/components/DailyWordCard.tsx
@client/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pronunciation Button + Audio Playback to DailyWordCard</name>
  <files>client/src/components/DailyWordCard.tsx</files>
  <read_first>
    client/src/components/DailyWordCard.tsx (current component — note existing imports on line 8, useAuth on line 64, pronunciation span on lines 316-319, apiFetch pattern on lines 76-84, audio playback pattern on lines 177-198, audioRef pattern on line 74)
    client/src/lib/api.ts (apiFetch signature — takes endpoint string and optional RequestInit; returns Response; handles auth token and 401 refresh)
    .planning/phases/11-word-phonics-tts/11-PATTERNS.md (frontend pattern section — pronunciation playback adaptation, language visibility check, button styling)
  </read_first>
  <action>
Modify client/src/components/DailyWordCard.tsx to add pronunciation playback. All changes are within the existing component function:

1. **Add import** — On line 8, add `Volume2` to the existing lucide-react import:
   Change: `import { BookOpen, Loader2, Music2, Play, Pause, RefreshCw, Sparkles, RotateCw } from "lucide-react";`
   To: `import { BookOpen, Loader2, Music2, Play, Pause, RefreshCw, Sparkles, RotateCw, Volume2 } from "lucide-react";`

2. **Add supported languages constant** — After the imports (before the QueueStatus interface), add:
   `const SUPPORTED_PRONUNCIATION_LANGUAGES = ["es", "fr", "de", "pt", "en", "it"];`
   This must match the backend's VOICE_MAP keys exactly.

3. **Add state and ref** — Inside the DailyWordCard function, after the existing `audioRef` declaration (line 74), add:
   - `const [isSpeaking, setIsSpeaking] = useState(false);`
   - `const pronunciationAudioRef = useRef<HTMLAudioElement | null>(null);`

4. **Add playPronunciation function** — After the existing `togglePlay` function (around line 189), add:

   `const playPronunciation = async () => { ... }` that:
   a. Returns immediately if `isSpeaking` is true (prevent double-click)
   b. Wraps in a retry loop: `for (let attempt = 0; attempt <= 1; attempt++)` (max 2 attempts per D-11-18)
   c. Inside try: calls `apiFetch(\`/daily-word/pronounce?word=${encodeURIComponent(data!.word.text)}\`)`
   d. If response not ok: parse JSON body, throw with body.error or "Pronunciation unavailable"
   e. Get blob from response: `const blob = await res.blob()`
   f. Create object URL: `const url = URL.createObjectURL(blob)`
   g. Create audio: `const audio = new Audio(url)`
   h. Store ref: `pronunciationAudioRef.current = audio`
   i. Play: `audio.play()`
   j. Set `setIsSpeaking(true)`
   k. Set `audio.onended` to: `setIsSpeaking(false); URL.revokeObjectURL(url)`
   l. Set `audio.onerror` to: `setIsSpeaking(false); URL.revokeObjectURL(url)`
   m. Return on success (break retry loop)
   n. In catch block: if `attempt === 1` (final attempt), show error toast — set a temporary state or use the existing `refreshError` state with message "Pronunciation unavailable" (D-11-19), then clear after 3 seconds via `setTimeout`

5. **Render pronunciation button** — In the JSX, AFTER the pronunciation `<span>` element (lines 316-319, the `{data.word.pronunciation && (...)}` block), add the speaker button. It should be INSIDE the same `<div className="flex flex-wrap items-center gap-x-3 gap-y-2">` container (line 315):

   ```
   {SUPPORTED_PRONUNCIATION_LANGUAGES.includes(user?.target_language || "") && (
     <button
       onClick={(e) => { e.stopPropagation(); playPronunciation(); }}
       className="p-1 rounded-full hover:bg-zinc-200 dark:hover:bg-zinc-800 transition-colors"
       aria-label="Listen to pronunciation"
       disabled={isSpeaking}
     >
       <Volume2 className={`w-4 h-4 transition-colors ${isSpeaking ? "animate-pulse text-zinc-900 dark:text-white" : "text-zinc-400 dark:text-zinc-500"}`} />
     </button>
   )}
   ```

   Key details per CONTEXT.md decisions:
   - D-11-06: Hidden when language not in SUPPORTED_PRONUNCIATION_LANGUAGES
   - D-11-14: `animate-pulse` class when `isSpeaking` is true
   - D-11-15: Returns to normal (no animate-pulse) when audio.onended fires
   - D-11-16: Placed right after the pronunciation phonics span
   - D-11-17: Uses Lucide `Volume2` icon
   - `e.stopPropagation()` prevents the parent button's flip behavior from triggering

6. **No loading spinner for pronunciation** — Per D-11-13, "Immediate play on click; show loading spinner only if >200ms delay." Since pronunciation is typically cached and plays instantly, do NOT add a spinner. The `animate-pulse` on the Volume2 icon serves as the "playing" indicator. If the first click doesn't produce audio within 200ms, the pulse animation already provides feedback.
  </action>
  <acceptance_criteria>
    - client/src/components/DailyWordCard.tsx imports Volume2 from "lucide-react" (line 8 contains "Volume2")
    - client/src/components/DailyWordCard.tsx contains `SUPPORTED_PRONUNCIATION_LANGUAGES` constant with value ["es", "fr", "de", "pt", "en", "it"]
    - client/src/components/DailyWordCard.tsx contains `isSpeaking` state declaration
    - client/src/components/DailyWordCard.tsx contains `pronunciationAudioRef` useRef declaration
    - client/src/components/DailyWordCard.tsx contains `playPronunciation` async function
    - client/src/components/DailyWordCard.tsx contains `apiFetch.*daily-word/pronounce` call inside playPronunciation
    - client/src/components/DailyWordCard.tsx contains `SUPPORTED_PRONUNCIATION_LANGUAGES.includes(user?.target_language`
    - client/src/components/DailyWordCard.tsx contains `animate-pulse` class conditional on isSpeaking
    - client/src/components/DailyWordCard.tsx contains `aria-label="Listen to pronunciation"`
    - client/src/components/DailyWordCard.tsx contains `URL.createObjectURL` and `URL.revokeObjectURL` in playPronunciation
    - client/src/components/DailyWordCard.tsx contains `"Pronunciation unavailable"` error message
    - client/src/components/DailyWordCard.tsx contains `e.stopPropagation()` on the pronunciation button onClick
    - Existing component behavior preserved: no TypeScript compilation errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ubuntu/lyric/client && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Speaker icon renders next to phonics for supported languages, clicking plays pronunciation audio via Blob URL, pulsing animation during playback, error toast on failure, existing component behavior unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Pronunciation Feature End-to-End</name>
  <files>client/src/components/DailyWordCard.tsx</files>
  <action>
    Human verification of the pronunciation feature:
    1. Start the backend server (`cd server && node index.js`) — verify Pocket-TTS daemon starts on port 3002 (check logs for health check success)
    2. Open the app in browser, navigate to the Daily Word card
    3. Verify a speaker icon (Volume2) appears next to the phonics/phonetic representation (e.g., /ˈho.la/)
    4. Click the speaker icon — verify pronunciation audio plays (you should hear the word spoken)
    5. During playback, verify the icon pulses (animate-pulse effect)
    6. After playback finishes, verify the icon returns to its normal state
    7. Change user's target language to an unsupported language (e.g., "zh") in the database or settings — verify the speaker icon is hidden
    8. Change back to a supported language — verify the icon reappears
    9. Click the speaker icon again — second click should be instant (cache hit)
    10. If Pocket-TTS is not running, click the speaker — verify "Pronunciation unavailable" toast appears
  </action>
  <acceptance_criteria>
    - Speaker icon visible for supported languages (es, fr, de, pt, en, it)
    - Speaker icon hidden for unsupported languages
    - Audio plays on click
    - Pulsing animation during playback
    - Error toast on TTS failure
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual verification — see task action for steps"</automated>
  </verify>
  <done>User confirms: speaker icon visible, audio plays, animation works, error handling works, no regressions</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express API | Pronunciation audio request crosses here; apiFetch handles auth token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-F01 | Tampering | URL.createObjectURL blob | accept | Blob URLs are same-origin, ephemeral, and revoked after use; no external resource loading |
| T-11-F02 | Denial of Service | Rapid button clicks | mitigate | `isSpeaking` state guard prevents concurrent requests; button disabled during playback |
| T-11-F03 | Information Disclosure | Error messages | accept | "Pronunciation unavailable" is non-technical; no stack traces or internal details exposed |
</threat_model>

<verification>
- TypeScript compiles without errors: `cd client && npx tsc --noEmit`
- Human verification: speaker icon visible, audio plays, animation works, error handling works
</verification>

<success_criteria>
- Speaker icon visible next to phonics for es/fr/de/pt/en/it languages
- Speaker icon hidden for unsupported languages (e.g., zh)
- Click plays pronunciation audio from /api/daily-word/pronounce
- Pulsing animation during playback, normal state after
- Error toast "Pronunciation unavailable" on failure after 1 retry
- No regression in existing DailyWordCard functionality (flip, song preview, queue status)
</success_criteria>

<output>
Create `.planning/phases/11-word-phonics-tts/11-02-SUMMARY.md` when done
</output>
