# Plan 14-07: Ops — tests, AI providers, Spotify quota

**Status:** Pending  
**Phase:** 14  
**Priority:** P2

## Objective

Make `deploy.sh` trustworthy; stabilize AI fallbacks; close Extended Quota / formal smoke leftovers from 12-11.

## Tasks

1. Fix Spotify `/status` test contract (`playback_scopes_ok`, redirect diagnostics).
2. Make pronounce test skip or mock when TTS daemon absent.
3. Review NVIDIA model IDs / OpenRouter free-tier 429 handling; curated path already works.
4. Document Extended Quota Mode for public Spotify users.
5. Optional: `deploy.sh --skip-tests` flag for known-safe VPS restarts.

## Success criteria

- [ ] `cd server && npm test` green in CI/VPS without local TTS flake
- [ ] AI cold-path failures degrade gracefully with clear logs
- [ ] Ops notes in `docs/SPOTIFY-INTEGRATION.md` current
