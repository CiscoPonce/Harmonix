---
phase: 14
plan: 14-07
title: "Ops: tests, AI providers & Spotify quota"
wave: 3
depends_on: []
files_modified:
  - server/src/routes/spotify.js
  - server/test/spotify.test.js
  - server/test/pronounce.test.js
  - server/src/services/ai.js
  - docs/SPOTIFY-INTEGRATION.md
autonomous: true
requirements_addressed: []
decisions_addressed:
  - D-14-07
---

# Plan 14-07: Ops — Tests, AI Providers & Spotify Quota

## Objective

Ensure backend test suite reliability (`npm test` 100% green), stabilize AI model failovers (NIM 404 / OpenRouter 429), and prepare Spotify Extended Quota submission documentation.

## Tasks

<task>
  <objective>Fix server unit test failures for Spotify status contract and TTS pronounce timeout</objective>
  <read_first>
    - server/test/spotify.test.js
    - server/test/pronounce.test.js
    - server/src/routes/spotify.js
  </read_first>
  <action>
    1) Update `server/test/spotify.test.js` to match the current `/api/spotify/status` response schema (`playback_scopes_ok`, redirect diagnostics).
    2) Update `server/test/pronounce.test.js` to mock TTS endpoint or skip gracefully when Pocket-TTS daemon on `:3002` is not reachable during automated test runs.
  </action>
  <acceptance_criteria>
    - `cd server && npm test` passes 100% of tests without hanging or failing
  </acceptance_criteria>
</task>

<task>
  <objective>Harden AI provider error handling for NVIDIA 404 and OpenRouter 429 responses</objective>
  <read_first>
    - server/src/services/ai.js
  </read_first>
  <action>
    In `server/src/services/ai.js`, catch NVIDIA NIM 404 (model unavailable) and OpenRouter 429 (rate limit) errors. Immediately log a clear warning and fall back to curated Deezer song/vocab catalog without throwing unhandled exceptions or timing out candidate queues.
  </action>
  <acceptance_criteria>
    - AI service catches 404 and 429 HTTP statuses gracefully
    - Curated catalog fallback delivers daily words when AI APIs are down/rate-limited
  </acceptance_criteria>
</task>

<task>
  <objective>Prepare Spotify Extended Quota submission guide in SPOTIFY-INTEGRATION.md</objective>
  <read_first>
    - docs/SPOTIFY-INTEGRATION.md
  </read_first>
  <action>
    Update `docs/SPOTIFY-INTEGRATION.md` with an 'Extended Quota Mode Submission' section. Document developer dashboard submission steps, requested scopes list (`user-read-private`, `playlist-read-private`, `playlist-modify-public`), screenshot/video demo requirements, and terms compliance checklist.
  </action>
  <acceptance_criteria>
    - `docs/SPOTIFY-INTEGRATION.md` contains Extended Quota submission checklist and instructions
  </acceptance_criteria>
</task>

## Verification

Run server unit tests:
```bash
cd server && npm test
```
