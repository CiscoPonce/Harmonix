---
phase: 14
plan: 14-06
title: "Standalone APK & release runbook"
wave: 3
depends_on:
  - 14-03
  - 14-04
files_modified:
  - docs/RELEASE-RUNBOOK.md
  - run_env.sh
  - deploy.sh
autonomous: true
requirements_addressed: []
decisions_addressed:
  - D-14-07
---

# Plan 14-06: Standalone APK & Release Runbook

## Objective

Deliver production release readiness: generate standalone Android APK (`app-release.apk`), update deployment scripts, and document full release procedures in `docs/RELEASE-RUNBOOK.md`.

## Tasks

<task>
  <objective>Create production release runbook covering VPS, ngrok, and Android APK</objective>
  <read_first>
    - run_env.sh
    - deploy.sh
    - docs/SPOTIFY-INTEGRATION.md
  </read_first>
  <action>
    Create `docs/RELEASE-RUNBOOK.md` detailing step-by-step instructions for: 1) VPS environment setup & environment variable validation (`JWT_SECRET`, `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`, `ENCRYPTION_KEY`), 2) Backend + Next + Pocket-TTS service launch via `bash run_env.sh`, 3) Public SSL endpoint verification via ngrok tunnel, 4) Flutter standalone release APK build (`cd mobile && flutter build apk --release`), 5) APK artifact distribution and verification.
  </action>
  <acceptance_criteria>
    - `docs/RELEASE-RUNBOOK.md` exists with end-to-end VPS deployment and APK build instructions
  </acceptance_criteria>
</task>

<task>
  <objective>Update deployment scripts for reliable VPS service restarts</objective>
  <read_first>
    - run_env.sh
    - deploy.sh
  </read_first>
  <action>
    Update `run_env.sh` and `deploy.sh` to ensure clean process cleanup on restart, verify Pocket-TTS port `:3002`, API port `:3001`, and Next.js port `:3009` availability, and add an optional `--skip-tests` flag to `deploy.sh` for fast ops restarts when tests have already passed in CI.
  </action>
  <acceptance_criteria>
    - `deploy.sh` supports optional `--skip-tests` flag
    - `run_env.sh` correctly starts all 3 services and checks process status
  </acceptance_criteria>
</task>

## Verification

Test deployment script flags and verify runbook creation:
```bash
test -f docs/RELEASE-RUNBOOK.md && grep -q "skip-tests" deploy.sh
```
