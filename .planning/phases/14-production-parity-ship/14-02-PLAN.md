# Plan 14-02: Spotify Connect UX (popup + Library)

**Status:** Pending  
**Phase:** 14  
**Priority:** P0  
**Source:** Phase 12.5 CONTEXT

## Objective

One-click Connect from Library (popup OAuth when possible; redirect fallback). Settings remains source of truth for disconnect/status.

## Tasks

1. Web popup OAuth + `postMessage` / allowlisted callback outcomes.
2. Library inline **Connect Spotify** (shared hook with Settings).
3. Optional onboarding prompt (web).
4. Operator runbook + tests for popup/Library paths.

## Success criteria

- [ ] User can connect without hunting Settings first
- [ ] No tokens exposed beyond auth start URL
- [ ] Fallback redirect still works when popup blocked

## Canonical refs

`.planning/phases/12.5-spotify-connect-ux/12.5-CONTEXT.md`
