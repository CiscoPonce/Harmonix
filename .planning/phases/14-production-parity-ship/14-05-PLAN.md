---
phase: 14
plan: 14-05
title: "Dual-frontend QA & Capacitor fallback retention"
wave: 2
depends_on: []
files_modified:
  - README.md
  - docs/DUAL-FRONTEND-QA.md
autonomous: true
requirements_addressed: []
decisions_addressed:
  - D-14-05
---

# Plan 14-05: Dual-Frontend QA & Capacitor Fallback Retention

## Objective

Execute dual-frontend QA matrix validating feature parity between Next.js Web and Flutter Mobile, and formalize Capacitor retention as a documented legacy fallback build alongside primary Flutter codebase.

## Tasks

<task>
  <objective>Create dual-frontend parity checklist document</objective>
  <read_first>
    - README.md
    - .planning/phases/10-mobile-dual-frontend/10-CONTEXT.md
  </read_first>
  <action>
    Create `docs/DUAL-FRONTEND-QA.md` documenting test scenarios and verification results across 7 core feature domains: 1) Auth (JWT login/register), 2) Discover (WOTD & catalog search), 3) Learn (daily word queue & active recall), 4) TTS (Pocket-TTS audio generation & cache), 5) Spotify OAuth & Library sync, 6) Harmonix export + match report, 7) Settings language editing.
  </action>
  <acceptance_criteria>
    - `docs/DUAL-FRONTEND-QA.md` exists with test matrix across both Web and Flutter
    - All 7 feature domains have defined verification criteria
  </acceptance_criteria>
</task>

<task>
  <objective>Update project README with primary Flutter build steps and Capacitor fallback docs</objective>
  <read_first>
    - README.md
  </read_first>
  <action>
    Update `README.md` to clearly specify Flutter (`mobile/`) as the official primary mobile app. Add a 'Mobile Development' section with Flutter setup/run commands, and retain a 'Legacy Capacitor Fallback' section explaining how to build the Capacitor Android APK wrapper if needed.
  </action>
  <acceptance_criteria>
    - `README.md` lists Flutter as primary mobile app
    - `README.md` contains legacy Capacitor fallback documentation per decision D-14-05
  </acceptance_criteria>
</task>

## Verification

Check markdown syntax and documentation files:
```bash
test -f docs/DUAL-FRONTEND-QA.md && grep -q "Capacitor" README.md
```
