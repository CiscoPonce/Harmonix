---
phase: 14
plan: 14-01
title: "Web polish & AppShell consistency"
wave: 1
depends_on: []
files_modified:
  - client/src/components/AppShell.tsx
  - client/src/app/discover/page.tsx
  - client/src/app/playlists/page.tsx
  - client/src/app/settings/page.tsx
autonomous: true
requirements_addressed: []
decisions_addressed:
  - D-14-06
---

# Plan 14-01: Web Polish & AppShell Consistency

## Objective

Finish web polish for the Harmonix design system (Linguistic Resonance): align Word of the Day card with design mocks (`design/learn-word-of-day.png`), wire global AppShell header search bar, implement Library persistent bottom player controls, and add Dyslexic font & stats toggles to Settings.

## Tasks

<task>
  <objective>Align Daily Word card spacing and typography with learn mock design</objective>
  <read_first>
    - client/src/app/discover/page.tsx
    - client/src/components/AppShell.tsx
    - design/learn-word-of-day.png
  </read_first>
  <action>
    Update `/discover` and Learn components to use display font Fraunces for target words and DM Sans for translations. Adjust card padding to 1.5rem, rounded corners to 12px, subtle green border glow `rgba(46, 125, 50, 0.15)`, and display clear audio preview button and word queue indicators matching design/learn-word-of-day.png.
  </action>
  <acceptance_criteria>
    - `client/src/app/discover/page.tsx` uses Fraunces display font for main word header
    - Daily Word card includes audio preview action button and target language badge
    - `cd client && npm run build` completes without errors
  </acceptance_criteria>
</task>

<task>
  <objective>Wire AppShell header search bar globally to Discover search API</objective>
  <read_first>
    - client/src/components/AppShell.tsx
    - client/src/app/discover/page.tsx
  </read_first>
  <action>
    Update `AppShell.tsx` search input to navigate to `/discover?q={query}` on submit or keydown Enter. In `/discover`, read search query param `q` from router/searchParams and automatically trigger song/lyric search via `/api/songs/search?q={query}` API endpoint.
  </action>
  <acceptance_criteria>
    - `client/src/components/AppShell.tsx` contains search submit handler navigating to `/discover?q=`
    - Entering a query into top header search bar updates Discover page with search results
  </acceptance_criteria>
</task>

<task>
  <objective>Implement persistent bottom player controls on Library page</objective>
  <read_first>
    - client/src/app/playlists/page.tsx
    - client/src/components/AppShell.tsx
  </read_first>
  <action>
    Add a fixed bottom now-playing strip in `client/src/app/playlists/page.tsx` displaying current track title, artist, album art thumbnail, play/pause toggle button, track duration progress bar, and volume/source badge (Deezer 30s preview vs Spotify).
  </action>
  <acceptance_criteria>
    - `client/src/app/playlists/page.tsx` contains bottom now-playing audio player controls
    - Player controls support play/pause toggle and track progress indicator
  </acceptance_criteria>
</task>

<task>
  <objective>Add Dyslexic font and learning stats toggles in Web Settings</objective>
  <read_first>
    - client/src/app/settings/page.tsx
  </read_first>
  <action>
    Add a 'Reading & Accessibility' section to `client/src/app/settings/page.tsx` with a toggle for OpenDyslexic / Dyslexia-friendly font styling (`font-dyslexic` CSS class toggle on body/container) and a toggle for showing detailed SRS memory strength statistics. Save preferences to localStorage or user settings API.
  </action>
  <acceptance_criteria>
    - `client/src/app/settings/page.tsx` includes Dyslexic font toggle switch
    - Toggling Dyslexic font adds/removes dyslexia font class on root element
  </acceptance_criteria>
</task>

## Verification

Run manual smoke test on Next.js dev server and verify build:
```bash
cd client && npm run build
```
