---
phase: 07-daily-word
plan: 01
type: execute
wave: 1
depends_on: [06-data-reliability]
files_modified: [server/db.js, server/services/aiService.js, server/services/dailyWordService.js, server/routes/dailyWord.js, server/index.js, client/src/components/DailyWordCard.tsx, client/src/app/page.tsx]
autonomous: true
requirements: [AI-02]
user_setup:
 - service: None
env_vars: [NVIDIA_NIM_API_KEY, NVIDIA_NIM_BASE_URL]
must_haves:
 truths:
  - "User receives one personalized word per day on opening the app"
  - "AI picks a real song containing that word and validates it against Deezer and LRCLib"
  - "User sees the exact lyric line where the word appears with audio preview"
  - "User can request a new word on demand"
 artifacts:
  - path: "server/services/dailyWordService.js"
   provides: "Generate-validate-cache loop for daily words"
  - path: "server/routes/dailyWord.js"
   provides: "GET /api/daily-word and POST /api/daily-word/new"
  - path: "client/src/components/DailyWordCard.tsx"
   provides: "Home screen word-of-the-day experience"
 key_links:
  - from: "server/services/dailyWordService.js"
   to: "server/services/aiService.js"
   via: "AI word + song generation"
  - from: "server/services/dailyWordService.js"
   to: "server/services/validationService.js"
   via: "Lyric sync validation before serving"
---

<objective>
Implement the core MVP loop: word-first daily learning. The AI generates one vocabulary word, finds a real song containing it, validates lyrics and audio, and presents the lyric moment to the user.

Purpose: Restore the original product vision — learn one word per day through real music context.
Output: Daily word API, caching, and home screen card with on-demand refresh.
</objective>

<tasks>
<task type="auto">
 <name>Task 1: Database & AI Generation</name>
 <files>server/db.js, server/services/aiService.js</files>
 <action>
 Add daily_words cache table and user learning preferences. Implement generateDailyWord() AI prompt for word + song pairing.
 </action>
 <done>Daily word schema and AI generation functional.</done>
</task>
<task type="auto">
 <name>Task 2: Validation & Alignment Service</name>
 <files>server/services/dailyWordService.js, server/services/dailyWordService.test.js</files>
 <action>
 Orchestrate AI generation, Deezer search, LRCLib fetch, lyric sync validation, and word-in-lyric alignment with retry loop.
 </action>
 <done>Validated daily word payload built and cached per user per day.</done>
</task>
<task type="auto">
 <name>Task 3: API Routes</name>
 <files>server/routes/dailyWord.js, server/routes/dailyWord.test.js, server/index.js</files>
 <action>
 Expose GET /api/daily-word (cached) and POST /api/daily-word/new (force refresh).
 </action>
 <done>Authenticated daily word endpoints live.</done>
</task>
<task type="auto">
 <name>Task 4: Frontend Daily Word Card</name>
 <files>client/src/components/DailyWordCard.tsx, client/src/app/page.tsx</files>
 <action>
 Home screen card showing word, translation, lyric snippet, audio preview, and new-word action with loading overlay.
 </action>
 <done>Word-first entry point visible on home page.</done>
</task>
</tasks>

<success_criteria>
User opens app and receives one validated word embedded in a real song lyric, with option to request a new word anytime.
</success_criteria>
